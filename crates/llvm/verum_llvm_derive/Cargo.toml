[package]
name = "verum_llvm_derive"
version = "0.1.0"
edition = "2024"
description = "Procedural macros for verum_llvm (LLVM 21)"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "fold"] }
