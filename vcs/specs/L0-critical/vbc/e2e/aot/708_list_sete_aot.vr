// @test: run
// @tier: 0
// @level: L0
// @tags: aot, list, set-element
// @expected-exit: 0

fn main() -> Int {
    let mut arr = [1, 2, 3];
    print(arr[0]);
    print(arr[1]);
    print(arr[2]);
    arr[0] = 100;
    print(arr[0]);
    assert_eq(arr[0], 100);
    assert_eq(arr[1], 2);
    0
}
