// @test: run
// @level: L0
// @expected-exit: 0

mount core.prelude.*;

fn main() -> Int {
    let s = "AB";
    let bytes = s.as_bytes();
    let n = bytes.len();
    print(f"bytes_len={n}");
    0
}
