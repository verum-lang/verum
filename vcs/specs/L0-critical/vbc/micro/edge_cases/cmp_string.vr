// @test: run
// @level: L0
// @tier: 0
// @expected-exit: 0
mount core.prelude.*;

fn main() {
    assert_eq("hello", "hello");
    assert("hello" == "hello");
    assert(!("hello" == "world"));
    assert("abc" != "xyz");
}
