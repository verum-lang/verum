// @test: run
// @level: L0
// @tier: 0
// @expected-exit: 0
fn main() {
    let x = (0b1010 | 0b0101) & 0b1100;
    assert_eq(x, 0b1100);

    let y = 0b1010 ^ 0b1111;
    assert_eq(y, 0b0101);

    let z = ~0b1010 & 0xFF;
    assert_eq(z, 0b11110101);
}
