// @test: run
// @level: L0
// @tier: 0
// @expected-exit: 0
fn abs(x: Int) -> Int {
    if x >= 0 {
        return x;
    }
    return -x;
}

fn main() {
    assert_eq(abs(5), 5);
    assert_eq(abs(-5), 5);
    assert_eq(abs(0), 0);
}
