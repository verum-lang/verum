// @test: run
// @level: L0
// @tier: 0
// @expected-exit: 0
fn compute(a: Int, b: Int) -> Int {
    a * b + a - b
}

fn main() {
    assert_eq(compute(3, 2), 7);
    assert_eq(compute(0, 5), -5);
    assert_eq(compute(5, 0), 5);
}
