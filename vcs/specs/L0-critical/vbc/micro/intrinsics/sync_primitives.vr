// @test: run
// @level: L0
// @tier: 0
// @expected-exit: 0
mount core.prelude.*;

fn main() {
    // Test basic synchronization patterns
    // Simple spin loop simulation
    let mut counter = 0;
    while counter < 10 {
        counter = counter + 1;
    }
    assert_eq(counter, 10);
}
