// @test: typecheck-pass
// @level: L0
// @tags: unsafe, while, loop
// @description: Unsafe block inside while loop body
fn main() {
    let mut sum: Int = 0;
    let mut i: Int = 0;
    while i < 3 {
        sum = unsafe { sum + i };
        i = i + 1;
    };
    assert_eq(sum, 3);
}
