// @test: run
// @level: L0
// @tier: 0
// @expected-exit: 0
mount core.prelude.*;

fn main() {
    let mut entered = false;

    while false {
        entered = true;  // Should never execute
    }

    assert(!entered);
}
