// @test: run-interpreter
// @level: L0
// @tier: 0
// @expected-exit: 0
// @description: Verify mixed literal and hex in byte strings
fn main() {
    let bs = b"A\x42C";  // ABC
    assert_eq(bs[0], 65);   // A
    assert_eq(bs[1], 66);   // B (0x42)
    assert_eq(bs[2], 67);   // C
}
