# Verum Technical-Debt Register

Status: LIVING DOCUMENT — single consolidated inventory of open debt
across every layer, with priorities and acceptance criteria. Produced
from a five-lane repository audit (conformance pins, compiler gap
surface, Rust hygiene, documentation drift, stdlib structure) on
2026-07-11; update rows in place as items close.

RECONCILED 2026-07-16 (T0102, five-agent audit): verdicts/anchors refreshed
row-by-row; every actionable row now carries its task-pool ID (`T0xxx`, see
`docs/architecture/multi-session-taskpool.md`) — claim the task before
working a row. New rows A22–A28 added from the audit.

Priorities: **P0** = wrong results or masked failures reach users;
**P1** = correctness debt with a bounded blast radius or a workaround;
**P2** = hygiene/polish that slows work but does not corrupt results.

---

## R. Release spine — fundamental-mechanism census (2026-08-02)

Ordered by fundamentality: each stratum unblocks the ones below it.
This is the NAVIGATION layer — the detailed rows/sections elsewhere in
this register stay authoritative for per-item state. Every entry
carries its pool ID; claim before working.

**R1. Identity (the root stratum).** Names are still the identity
carrier in 679 measured places (census ratchet:
`scripts/ci/census_name_keyed_surfaces.py`). Campaign: T0690
(DefId end-to-end, S0/S1 landed, S2+ open), T0691 (ONE symbol-graph
artifact), T0448/T0215-class residuals. Until S2 lands, every
same-name collision class (barename ctor capture, qualified-key
squats, first-wins registries) stays reachable.

**R1b. DUPLICATED WALKS — measured 2026-08-08.** The identity problem
has a structural twin: the *traversals themselves* are duplicated, so a
correct mechanism wired into one copy stays silent in the others. Four
independent instances measured in one day:

* **Declaration walk ×3** — `phases/semantic_analysis.rs` (not invoked
  by `verum check` at all), `pipeline/cross_file.rs::analyze_module`
  (traced: does not run for a single-file check),
  `compile_orchestration.rs::check_project` (what `verum check` actually
  runs). Blocks T0656: the 82-attribute declarative registry in
  `verum_types::attr` has ZERO consumers, and wiring it into any ONE
  driver buys a working `check` and a silent `verify`/build.
* **Variant-body registration ×3** — `infer/helpers.rs` (lazy archive
  load) plus two sites in `infer/decls.rs`, each minting its own
  persistent vars for the SAME parameter. Measured: `type_var_order[Maybe]`
  holds `TypeVar(19838)` while `ctor.args` holds `TypeVar(18417)`, so a
  constructor comes out as `fn(Var a) -> Maybe<Var b>` with `a != b` —
  the argument binds `a`, the head stays free, and it surfaces far away
  as `E404 … Maybe<_> is not fully determined` (162 red in base/maybe,
  181 in base/result, 74 in base/protocols). Seven substitution-side
  fixes were tried and all were correct-but-irrelevant: they reconcile
  vars WITHIN one registration while the divergence is BETWEEN them.
* **Attribute registry ×2** — the declarative one above vs a 61-name
  hardcoded copy in `verum_fast_parser::attr_validation`, wired for 2 of
  7 targets with its warnings read only by a test.
* **Module-name publication ×3** — the glob leg of T0691, landed
  069f2ed88. The authoritative enumeration
  (`metadata_known_module_items`) existed but drove only E401's
  "module exports:" *text*, while mounts walked a synthesized mirror
  that had drifted.
  * **Continued, measured 2026-08-31.** The split did not close, and a
    third mechanism sits in front of both. Three legs:
    * **A module's surface lost types by DECLARATION FORM** (T1002,
      fixed). `core/security/tuf/types.vr` declares seventeen public
      types; the compiler listed 24 names for that module and `KeyId`
      from line 146 of that same file was not among them. Four codegen
      arms built their `TypeDescriptor` through the struct-update
      default, leaving `origin_module` at `None` — and `None` is not
      neutral: the archive writer then attributes the type to the
      archive ENTRY path. The costly form is the least suspicious one,
      `type X is (T);`, which parses as a ONE-ELEMENT TUPLE and lowers
      through the `Tuple` arm. Radius measured over 90 single-line
      newtypes: 18 could not be mounted by the path their own file
      declares, 72 could — the fallback is accidentally right whenever
      a file IS its module's entry. Fixed: 17 of the 18 (the 18th is
      `cog.resolve`, unreachable because `cog` is a keyword — T1008);
      0 of the 72 moved.
    * **The verdict depended on how the path was SPELLED** (T1005,
      fixed with T1006). One module `core.security.tuf.types` had two
      surfaces — `probed-exports` answering with 61 names,
      `metadata_known_module_items` with 24 — and the presence of a
      `core.` prefix decided which was asked. Both rejected a
      merely-imported name, so the larger was never "everything visible".
    * **The stdlib top-level list was hardcoded and incomplete** (T1006,
      fixed). `verum_types/src/infer/mod.rs` held 49 `core/` subdirectory
      names as a constant against 52 actual: `hash`, `id`, `mac`,
      `random`, `script`, `subtle` were absent, so
      `mount id.snowflake.{SnowflakeError}` answered E402 "module not
      found" while the `core.`-prefixed form resolved. The comment there
      knew the no-hardcoded-stdlib rule, called the violation "a clarity
      choice", and justified it with a claim true in only one direction —
      every name IS a directory, but not every directory is a name, and
      nothing re-checked the converse as `core/` grew.
    The cure for both was one: ask an AUTHORITY instead of a list —
    registry first (so a project module keeps its own identity), then the
    archive metadata via a prefix range. Measured rather than assumed at
    each step: an earlier registry-only attempt was INERT (`core.id` is
    absent from the registry under every spelling, which also explains
    why `mac.hmac` worked without being listed — it IS in the registry);
    before removal, `in_list && !registry && !metadata` was false for 48
    of the 49 names and the 49th (`cog`) was unreachable as a path prefix
    because it is a keyword (T1008); after removal, all 49 answer E401
    rather than E402, and bare vs `core.`-prefixed agree 49 of 49.
* **Execution pipeline ×2 — the conformance suite runs the OTHER one**
  (T0732, measured 2026-08-14). `verum run` calls
  `run_interpreter` (`pipeline/dispatch.rs:316`): stdlib + project +
  external-cog loads, parse, **safety gate, type check, resolved-call-target
  application, dependency analysis, verify, CBGR analysis**, interpret.
  Every `@test: run-interpreter` spec instead reaches
  `run_for_test` (`pipeline/interpreter.rs:392`): stdlib load, parse,
  meta registration, macro expansion, interpret — **none of the six
  phases in bold**. Measured consequence on one file, one binary:
  `verum run vcs/specs/L0-critical/vbc/e2e/627_*.vr` prints
  `-140734938218496`, the runner reports PASS. Ruled out as causes: the
  runner's stdout comparison (breaking `@expected-stdout` fails the test)
  and stdlib-source-vs-archive (`VERUM_STDLIB_PATH=core` reproduces the
  same output). Without CBGR analysis a `&v` never becomes a reference, so
  a whole defect class is structurally invisible to the suite. Adjacent
  measurement, same day: 124 specs WRITE an `iter().<adapter>(…)` chain
  and 10 of them execute — which is how T0729 (eager `map`/`filter`
  breaking every chained adapter) survived. Unifying the two is expected
  to turn a significant share of the suite red, because those greens are
  vacuous; size the wave on a sample before landing.

Adjacent measured class, same file-level census (2026-08-08): **20 type
walkers in `verum_types` recurse through `Tuple`/`Reference` but drop
`Variant` and/or `Record` into `_ => ty.clone()`** — 4 in
`dependent_match.rs`, 6+ in `protocol.rs`, names like
`substitute_type_in_type`, `apply_substitution_to_type`,
`apply_type_substitution_impl`. Each claims to apply a substitution *to a
type* and silently returns its input unchanged for the two composite
shapes that carry a sum type's payloads. Two were completed under
ae2a2cbd6 (`replace_named_with_var`, `substitute_type_vars_fresh`); the
remaining ~18 are UNMEASURED — it is not yet established which of them
ever receive a `Variant` at all, and a walker that never does is
harmless. Do not batch-fix: measure which sites actually see sum bodies
first, because adding recursion changes behaviour wherever a type is
currently returned whole.

Diagnostic signature of the class: a fix in the obviously-correct place
changes nothing, because a *different* copy is the one executing. Verify
that the site RUNS (env-gated trace) before concluding a fix is wrong —
and keep a probe that MUST fail, since a dead check and a working one
are indistinguishable from a one-sided green.

**R2. Descriptor fidelity (bake ≡ source).** The archive's
signature-string channel erased declared identity; v2.10
PARAMNAME-CARRY (T0701, 11904454b) closed the inherent-method leg
(E404 adapter class dead). Open legs: protocol MethodSignature
surface T0708 (bounded-var dispatch E404 `_`), qualified-keyed
`metadata.types` (T0691 stone), bake-vs-user pipeline unification
T0692.

A descriptor field can also be the ONLY carrier of a fact: the lazy
symbol closure is computed from source TEXT, before inference, so
`f"{a.cmp(b)}"` reached `Int.cmp` but not the `Ordering` it returns —
and `implement Display for Ordering` stayed out of the archive, making
the f-string print the variant name `Less` where the impl says `<`.
`FunctionDescriptor.return_type` is what closes it, because it records
the type BY NAME; archive TypeIds are assigned per module and are not
comparable across them, which is why two attempts to read the type off
the bytecode were inert. Seeded only for calls in FORMAT POSITION
(spec 632): seeding from every reached function instead cost a
hello-world's archive load 1336 ms against 67 ms.

**R3. Type-system authorities.** One projection reducer instead of
three — T0707 (Item<Range>-argless residual, base/iterator 54 E400).
Mount-scope candidate discipline for multi-source names T0710
(T0525/T0704 disarm on >1 source). Match-guard exhaustiveness gate
T0649. Sized-int value semantics T0611 — **CLOSED 2026-08-08** (c185b51c6 + 9c73789f5): casts mask, `+`/`-`/`*`/`<<` wrap at the declared width, and sized-int METHOD dispatch honours the receiver's width. Seven roots in one chain, each hidden behind the one before it; SHA-256 and HMAC-SHA256 are byte-exact against FIPS 180-4 / RFC 4231 (0 mismatched bytes of 32).
E404 recovery shapes T0585/T0702. NLL dataflow T0694.

**MEASURED 2026-08-08 — what a full conformance pass costs.**
`verum test --interp` selects **18,589 of 18,958** tests; individual
tests run **15-16 s**, so one pass is **≈5 hours on 16 threads**, after
a ~5-minute silent discovery phase. Every guessed timeout under an hour
produces a ZERO-BYTE file, which reads identically to a hang or a crash
— two attempts (50 min, 110 s) did exactly that before the run was
given a start event to announce itself.

This bounds what `make check-inventory-live` can be. At five hours it is
not a per-PR gate in its current form; the honest options are chunking
with accumulated results, or a declared subset that states what it does
NOT cover. The gate now refuses a truncated stream outright — it
compares the suite's `selected` count against the `test` events that
arrived — so a partial run can no longer pass as a small green one.

**R3b. THE CHECKER CHECKS SIGNATURES, NOT DECLARATION INTERIORS —
measured 2026-08-18.** A name or a bound is verified where it appears in
a function signature and unverified where it appears inside a
declaration. Every row below was measured against a control in the same
batch, so the split is the finding, not the absence:

| position | unknown name / unsatisfied bound |
|---|---|
| `let x: Nope`, parameter, return, variant payload `A(Nope)` | E101 |
| `fn f<T: NopeProto>` inline bound, no `implement` at all | E405 |
| missing method on a value, missing field, 3 segments from a VALUE | E400 / E404 |
| local call with wrong arity | E102 |
| **record field, alias right-hand side, generic argument in a field** | **silent** |
| **`where T: P`** | **silent** |
| **protocol method return type and parameter type, associated-type bound, `type G<T: P>`** | **silent** |
| **`implement P for R { }` missing a required method** | **silent, then panics at the call** |
| **`a.b.absent()` — 3+ segment module path, missing leaf or middle** | **silent, returns `nil`** |
| **arity through any module path, at any length** | **silent, returns `nil`** |

* **T0806** — a module-path call of two or more segments is not resolved
  and its arity is never checked; `nil` satisfies any declared return
  type. 459 module-rooted calls in 142 files; 13 name a callee declared
  nowhere. Gates: `check-dead-module-path-calls` (13),
  `check-platform-call-parity` (130 resolved / 32 not). Consequences
  already measured: T0807 (heap pointer-encoding keys never seeded on any
  platform — every arm calls something absent), T0808 (the default panic
  handler neither prints nor aborts on macOS), T0809 (park/unpark bypass a
  futex layer that exists on all three platforms).
* **T0811** — a type path's qualifier is DISCARDED and only the leaf
  resolves, so `a.zzz.AlsoAbsent` becomes a phantom type named after
  itself. This is the mechanism behind T0796: four X.509 files declared
  certificate validity fields as `core.encoding.der.Time`, a module that
  declares `DerTime` and no `Time`, and the fields were typed `Unit`.
* **T0812** — conformance is not enforced, only existence. An empty
  `implement` silences the E405 that catches the same mistake when the
  block is absent. 12 instances; gate `check-protocol-conformance`. Four
  are `Debug` implementations providing `fmt` where the protocol requires
  `fmt_debug`.
* **T0813** — a `where` bound is unchecked while the inline form is
  checked. With ONE implementation of the protocol in scope the call
  dispatches to it, so a non-conforming type silently receives another
  type's method — a wrong value, not a panic. 434 `where` lines in 51
  files carry Clone, Ord, Eq, Send and kin.
* **T0805** — a gated block is a statement, so a function whose body ends
  in one yields `Unit`. 43 sites measured, 27 fixed, gate
  `check-cfg-block-tail` ratchets the remaining 16.

Two more of the same family sit outside the type system proper: T0814
(a method call inside a format substitution returns another method's
value, deterministically, while the plain call is correct) and T0815 (a
member named `size` read off a local aborts in global_ctors before main).
T0816 is the specification half — `grammar/verum.ebnf:1534` gives the
three bitwise operators one flat precedence level while the compiler uses
the C ladder, so the authoritative grammar predicts a different VALUE.

**R4. Execution truth (both tiers, loud-by-default).** CallM
const-zero degrade class A1/T0103-residue (strict-mono default =
T0693). AOT adapter lowering T0260. FFI placeholder arms A9/T0110,
SIMD degrades A10/T0112, Tier-0 ptr provenance A19/T0108. Prim-mangle
totality T0695 + post-typecheck harvest T0706. cbgr_test SIGSEGV
T0705.

**R5. Gates that actually gate.** ~~CI runs only `--lib --bins`~~ —
**LIVE since 2026-08-03 (T0709)**: the `integration` job arms
11,182 measured-green `tests/` guards (light-8 4,363 + fast_parser
4,158 + types 1,124 unskipped + verification 1,537). The first two
sleeping pins it exposed are already adjudicated: T0713 (affine pin
— falsified, enforcement was live on the diagnostics channel) and
T0712 (attestation debt, real). Pending tiers: ~~smt (one 10ms timing
pin needs headroom)~~ — **CLOSED 2026-08-30 (T0955, de5b65ed1)**: it
needed no headroom. `[profile.dev]` left DEPENDENCIES unoptimised, so
`cargo test` linked a Z3 compiled at `opt-level = 0` and the pin was
measuring a solver nobody ships. Found by `sample` — the top
non-waiting leaves were `assertions_enabled()`, `tag_enabled(TraceTag)`
and atomic bool loads. 14.41ms -> 0.86ms against the unchanged 10ms
threshold; a verification obligation end-to-end 18.0ms -> 6.3ms. vbc
(T0714 envelope-rewind pins), AOT-heavy nightly lane (draft below). INVENTORY liveness T0220. FORCE_VERIFY
sweep T0278.

**R5b. Release-blocking closures, 2026-08-14.** Four things that made a
release claim unsupportable are measured shut:

* **The suite runs the shipped path** (T0732, 4802c2acb). `run_for_test`
  skipped the safety gate, type check, resolved-call-target application,
  dependency analysis, verify and CBGR analysis — six phases `verum run`
  performs. Measured before: `verum run` on spec 627 printed
  `-140734938218496` where the runner reported PASS. The wave was measured
  before landing, not argued: 25 output-asserting specs went 16 → 18 passed
  across clean runs. `scripts/ci/check_shipped_path_parity.py` ratchets the
  residue (1 divergence, the async gap T0734).
* **The compiler no longer guesses a field position** (T0723, e108aebb0).
  FIELD-GUESS-HARD-1 is 0, down from 167; refusal is the default and
  `VERUM_FIELD_GUESS_PERMISSIVE=1` is the escape. Verified by re-baking under
  the new default to byte-identical totals.
* **A script behaves the same on its second run** (T0737, eecbe6ee4). The
  script cache returned modules with `global_ctors` dropped by serialisation
  and `resolved_band_map` / `resolved_protocol_dispatch` never recomputed;
  ten module tables now ride the wire and the load path runs the same three
  resolution passes the compile path does.
* **The shipped artifact has a size gate** (0a84c6a77). Fixing the above took
  the embedded archive from 20.9 MB to 474.5 MB with every gate green — two
  program-level tables written per archive member. `make check-archive-size`
  now bounds it above AND below.

Adjacent measured win: a program's compiled module no longer depends on what
its local variables are CALLED (T0738, dbaf118d6 + 3f493360e) — `let data`
cost 62980 functions against `let zzz`'s 12604, because the AST harvest fed
local bindings into a scan that decodes all 574 archive modules per name.
Spec 627's first build went 26.2s → 1.96s.

Still open on the release spine: `async fn` produces a value where the
typechecker promised a Future at Tier 0 (T0734), AOT `fold` with a closure
returns a pointer (T0728), a protocol-method chain silently drops the
adapter (T0731), and an unidentified flake in the ONE surface CI gates
(T0740).

**R6. Verification honesty.** `verify` reports Proved on a violated
precondition T0657; higher-order reflection T0490; excluded-middle on
opaque calls T0487; five ProofTerm types T0637; refinement
value-free-validity class (register §refinement).

**R6-addendum, MEASURED 2026-08-30 — a red verification spec has FIVE
distinct causes, and they must not be added together.**

The L3 proofs corpus stands at 27/71 specs, 265 unproved obligations
(from 23/68 and 275 the same day). Triaging what remains gave five
kinds of red, only the first of which is prover debt:

1. **A weak tactic or a missing theory.** The real debt: list theory
   (`len(xs.concat(ys)) == len(xs) + len(ys)` and its family, 14 goals),
   Peano over a user `Nat`, induction, existential witnesses.
2. **A compiler defect.** A `cases` block built its goal from the
   SCRUTINEE, so a proof structure destroyed a goal the solver closes
   in 10ms (T0960); a generic function's definition was declared over
   its type parameter's sort while the goal's call declared the same
   name over the instantiated sort, so the definitional axiom described
   a different function (T0980).
3. **The spec asserts something its author did not mean.** `==` binds
   tighter than `&&`, so `p && true == p` is `p && (true == p)` — false
   at `p = false`. The verifier refused correctly and the refusal was
   counted as prover weakness. A census over the corpus for that shape
   found 18 sites, of which 3 were mis-grouped.
4. **The spec declares its expectation where the runner never looks.**
   `// @test: verify-fail` on an individual theorem inside a file whose
   own directive says `verify-pass`. vtest reads the file's first
   directive and nothing else, so six theorems across three tactic
   files were punished for declining correctly.
5. **The verifier answers FALSE** — the worst, and the one that makes
   the other four cheap by comparison.

Two instrument defects were found while measuring, and both would have
falsified the numbers: `cargo build -p A --bin X -p B` builds only `X`
(the `--bin` filter silences every other `-p`, exit 0, no warning), so
a suite run by `B`'s binary measures the previous build; and a full
disk wears other defects' symptoms — a name-resolution census taken at
100% disk did not survive re-measurement and was retracted.

Structural finding, filed as T0983: the parser accepts a theorem body
with NO closing brace, silently. Four of 68 files in the proofs corpus
are unbalanced; `blast_tactic.vr` is missing 24. Bodies close on the
next keyword, so nothing shows until a file is cut. A spec whose
structure depends on error recovery is a spec whose content is decided
by a heuristic rather than by its author.

**R7. Product surface.** Format-spec pipeline T0700; effect
polymorphism T0696; QTT channels T0697; region polymorphism T0698;
contract-carrying archives T0699 — the innovation tier, after R1-R6
hold.

**R-addendum (2026-08-05 session census — systemic classes surfaced
while closing T0272/T0555/T0165/T0463/T0523/T0424/T0438/T0485).**

* **R1 residual, ACTIVE:** the receiver-qualified method reader-flip
  landed (T0165) and archive Sum types now register their variant
  ctors (ARCHIVE-VARIANT-CTOR-1, T0555) — but a bare variant mounted
  through a RE-EXPORTING module still dies in VBC codegen
  (`undefined variable: UoInsert`), and the module-surface answer for
  one path comes from THREE unreconciled sources (`ModuleInfo.exports`
  / `module_reexports` / `metadata_known_module_items`). ONE
  module-surface authority belongs in the T0691 stone.
  **One leg of that drift closed 2026-08-09 (T0691):** the synthesized
  archive surface was built from the three descriptor families and
  never from `module_reexports`, so a RENAMED re-export
  (`public mount .constraint.{LayoutConstraint as Constraint};`)
  existed in the metadata and nowhere in `ModuleInfo.exports` — the
  probed-exports E401 gate rejected the mount against a 263-entry
  surface missing exactly the leaves. The leaves now fold in, with the
  kind read off the TARGET and an unresolvable leaf skipped rather than
  guessed (`pipeline/loading.rs`). Paired measurement, same invocation
  both sides: term/layout 20 failed → 0; sync/mod, text/text,
  intrinsics/arithmetic, base/mod, collections/list unchanged at 0.
  The drift is NOT gone — three divergences remain and are named on
  T0691: variant CASES present in one surface and absent from the
  other; inherent METHODS published into the surface that three
  consumers ENUMERATE (`core.term.layout` exports `Rect.top`); and the
  AST-derived table for project modules, which drops `ItemKind::Mount`
  entirely via `_ => Ok(())`.
* **R2 new class, half-closed:** archive entry granularity is the
  DIRECTORY module, so every type in a multi-file stdlib module lost
  its declaring file submodule — v2.12 `origin_module` carry landed
  (T0555); the remaining half is consumers that still key on the
  entry path.
* **R-minor, measured 2026-08-09:** refinement reflection skips any function whose body references a MODULE CONSTANT — `WARN refinement reflection: skipping `ordering_to_u8` — its body references `ORDERING_RELAXED`, which is neither a parameter nor another reflected function`. Verification-side only (the body still compiles and runs); it narrows what the reflection layer can reason about, and it fires on every check of core/sync/atomic.vr. The reflected-term grammar needs a constant-lookup arm.
* **R4 missing artifact:** Tier-1 has no written VALUE-MODEL contract.
  The i128 wide-slot model added a fourth way registers carry facts
  (the LLVM value's own type, beside f64 slots, class marks and
  prescan sets), and the i128 call-boundary is a documented residual —
  a one-page "what form crosses a call/collection boundary per type
  class" contract is the cheapest way to stop per-type re-derivation.
* **R5 additions, LIVE:** three arch-gates landed this session —
  panic-surface ratchet (T0424, baseline 664), dup-emitter +
  dead-libc-body census with runtime definer backstop (T0438; its Arm
  B immediately found a LIVE breach: EnvGet lowers to libc `getenv`
  with no syscall leg — tied to T0310's env leg), and the
  bool-eq-in-conjunction lint (T0485, both acceptance halves as live
  `--lib` pins).
* **R5 tooling class, NEW:** instruments that report unverified
  success keep recurring — the script VBC cache ignored the stdlib
  snapshot (T0523, closed: `resolved_stdlib_identity` in both cache
  keys) and `VERUM_CORE_PATH` was silently inert for `verum run` (now
  warned). Standing rule worth a gate: every cache key names EVERY
  input the cached artifact was built from.
* **R6 update:** the verify spine is honest end-to-end now — T0657
  (woven wp obligations + Labeled blame) and T0671
  (thorough/certified mandatory termination) are closed on main.
* **R-infra, NEW:** parallel sessions serialize on the HARDCODED
  shared `target/precompile-bootstrap` (observed 1h23m flock wait),
  and the bake cache key is a hand-curated file list — a missed entry
  silently re-embeds a stale archive. Candidates: session-scoped
  bootstrap target dir; derive the key set from the cargo graph
  instead of curation.

---

## A. Compiler / runtime (Rust crates)

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| A1 | **CallM const-zero degrades** — unresolved method calls lower to `dst = 0` silently (measured 4210 sites in a hello-world binary). Root cause named in-code: VBC-GENERIC-INSTANTIATION (no generic-monomorphization-context propagation). Strict gate exists but is opt-in. **2026-07-16: STILL OPEN, anchors refreshed (T0103).** Fresh measurement (same day, isolated HEAD build): a trivial 40-line probe still reports **1026** unresolved calls degraded to const-zero at AOT lowering (down from the historical 4210 — mono-default-ON helped — but three digits of silent-wrong-result surface remain). **2026-07-25 re-measured (99f6b93e) — the trend has NOT improved:** a THREE-LINE program mounting only the prelude (`let xs: List<Text> = ["alpha","beta","gamma"]; xs.contains(...)`) already emits **805** `CallM method X unresolved at codegen — degraded to a const-zero stub` warnings under `--aot`. And the silent-wrong consequence is now demonstrated, not just counted: that program's `.contains(&"beta")` returns **true at Tier-0 and false at Tier-1** — `contains` compares via a protocol method on its element type, so the const-zero stub makes it constant-false with no diagnostic at the call site (T0332, re-pointed at this row and raised to P1). **2026-08-01: T0332's user-visible legs are CLOSED** — `contains`/`index_of` route element compares through `verum_generic_eq` (structural+text authority) with SHAPE-AWARE needle deref (a Tier-1 `&T` is bimodal: scalar refs are slot addresses, heap refs are the object pointer — the `ref_param` mark discriminates; blanket deref read a Text's first field as the value → false at rest, SIGSEGV when chased); `index_of` now honours its declared `Maybe<Int>` contract (Some/None diamond — the raw-index return crashed `Maybe.fmt`); rodata Text literals carry 16-byte alignment so the structural probe accepts them; `first`/`last`/`MakeVariantTyped` share ONE `stamp_variant_header` authority. The const-zero DEGRADE class itself (this row) remains open. | P0 | `llvm/instruction.rs:9759` (UNRESOLVED_FN_ID→const_zero), `:19098` (CallM unresolved), gate `llvm/error.rs:668` | `VERUM_STRICT_MONO=1` clean on the core-tests corpus; then flip strict to default | **2026-08-11 (T0384 campaign): the STATIC-CALL leg localised.** A type-qualified call compiles to `LOAD_K String("TypeName")` + `CALL_M "method"`, so the receiver is a small string — neither a primitive nor a pointer. Every dispatch arm after `C-pre-variant-1` gates on `is_ptr()`, so the call falls out of the chain and the destination keeps its default: `Shared.new(42)` returns an EMPTY value and `Shared.strong_count(1)` returns 0, both with no diagnostic. `VERUM_STRICT_FN_ID` / `_MONO` / `_CODEGEN` were tried and NONE of them reaches this path — the register's "strict gate exists but is opt-in" does not hold for static calls. This blocks the approved campaign to delete the interpreter's private `Shared` representation: while an unresolved call answers 0, no resolution work is verifiable, because every failure looks like a success.
| A2 | ~~Tensor/GPU unknown sub-ops silently write 0~~ **CLOSED 2026-07-11**: both fallthroughs now route to `emit_unimplemented_sub_op("TensorExtended"/"GpuExtended", sub_op)` before the keep-alive const-zero — gaps surface in diagnostics instead of miscompiling silently. | ~~P0~~ done | fix commit on main (`lower_tensor_extended`/`lower_gpu_extended`) | ✅ fallthroughs report |
| A3 | **Lenient-SKIP BugClass warnings** (#166). BURN-DOWN 2026-07-12: **176 → 132 → 82 → 64 → 53 → 39 → 31 → 21** (seven consecutive zero-new bakes) (bakeX/Y/Z all zero-new). bakeX −50: arity ×5, f-string-capture class (~30 fns incl. parse_*/get_*/select/QuicServer.bind), wave-5 ×17 files. bakeY −18: wave-6 onion ×9 files (match-arm-RHS bare variants — arm-filter blind spot; single_int0_i64 = typo'd nonexistent fn → single_int(0)), wave-6b receiver-drift impls (GPUDevice ComputeDevice statics read self with no self param; is_covering dropped protocol's &self; commutativity_coherence self.act in receiver-less family), wave-6c @cfg(macos) block-scoped fd + peer_cred super.darwin.* mis-root, progressive core.-suffix resolution (resolve_core_rooted_suffix: longest registered proper suffix ≥2 segments, wired into qualified-path/field-access/method-call — closes 'undefined variable: core' in ALL expression shapes). bakeZ −11: FreeVarAnalyzer EXHAUSTIVE ExprKind coverage (39 of 73 kinds fell into the capture-dropping catch-all — select/comprehensions/spawn/try*/…; catch-all DELETED so a new ExprKind fails compile), pattern-position canonicalization (module-path-to-variant), phantom names materialized (ExtIoErr ctors) or corrected (WfText→FmtText, single_int0_i64→single_int(0)). CLOSED SINCE: stage-1 was BLIND to builtin-TypeKind impl targets ('implement Text' parses as TypeKind::Text, not Path — 694 stubs, zero Text.*; extractor now maps builtin kinds) → all 16 Text.* consumers green; phantom-API drift class realigned (Channel.unbounded→unbounded_channel, Duration.seconds→from_secs, from_bytes_lossy→from_utf8_lossy) + two GENUINE owner APIs added (Text.from_cstr on load_byte intrinsic; Instant.from_nanos) + BinaryHeap added to canonical set. CLOSED SINCE: **stage-5 mount-miss named stubs** (explicit braced-mount item + producer-not-yet-compiled + simple name NOT globally unique → call-site-synthesized stub with the call's arity under the mount's FULL qualified path; ArchiveBodyRemap name-chase resolves unambiguously; new STAGE5 band in verum_vbc::stub_ranges) — open_admin family, flags_default, access_name, kqueue/kevent/open, hedberg all green; jwt.decode_header_only phantom materialized at owner (OIDC key-selection primitive). REMAINDER 21→(bakeAJ pending): PROOF-IRRELEVANCE-ERASURE-1 landed (axioms callable as erased witnesses: LoadTrue for Bool props, LoadNil for dependent proofs — hott x4 + hedberg + ua + infinity_topos partially; truth stays with verification phase) ≈ −7; uppercase-locals RECLASSIFIED into (c) via bakeAI [undef-var] evidence (params AND lets lose bindings inside meta-generic impls — var_reg_err, is_type_name=false: ssm B/B_bar, nn H, rag neighbors — peer v11 impl-generics-carry domain, N/DIMS/k same root). B1 layer-2 ROOT FIXED: ALIAS-INTRA-MODULE-ORDER-1 — declared_alias_target_name's extra type_name_to_id check dropped `type IoError is StreamError;` (mod.vr evaluated before sibling protocols.vr registered the target; bakeAI [static-alias] alias_hit=false witness); local-visibility scope gate is now the sole evidence when the oracle is present. Classes:  (c) meta const-generics N/DIMS/k ×9 (peer impl-generics-carry pillar); NEW (f) uppercase locals B/B_bar/A_scaled ×3 mis-classified in expr position (ssm.vr params — needs trace); NEW (g) proof-surface calls (axiom-as-term hott.vr ×3, protocol-field functor composition infinity_topos ×2) — verification-layer design item. Flip-to-hard-error gated on (a). || A5 | **Shared-identity / payload-extraction fork** — extracting `Shared<T>` from an enum payload copies the cell; cross-handle shared mutable state is impossible from .vr. Blocks 9 pinned tracing-pipeline tests and any stateful in-memory pattern. **2026-07-20: CLOSED interp (T0107, c8f6205aa).** The filed premise ("extraction copies the cell") was DISPROVEN by the fixer — the real root was a read/write asymmetry: `SetF` had no `Shared`-deref arm, so writes corrupted the cell directly. Interp fixed + verified (mem/allocator 65/65). Residuals: T0384 (Shared carrier minors) open, T0393 (AOT Shared-carrier deref) open. The 9 tracing/pipeline pins were reattributed to T0146 (DONE) but stayed red on a DISTINCT root (baked Shared-field dispatch in `core/tracing/data.vr`) = **T0468**, so they need a re-run, not a re-diagnosis. | P1 | T0384/T0393/T0468 | tracing pipeline pins re-run under T0468 |
| A6 | **Protocol-object (dyn) dispatch inoperative in the interpreter** — all receiver forms mis-bind. Stdlib now routes through closed-world handle enums; `…Custom(Shared<dyn …>)` seams are dormant. **2026-07-20: CLOSED (T0106, 94e212dae; AOT twin T0371).** Interp is now fully CORRECT for both `Heap<dyn P>` and `Shared<dyn P>` (not merely "loud") — carried-fact `resolved_protocol_dispatch`, verified against an 18364-test full interp sweep, 0 genuine-new regressions. The AOT residual (dyn-dispatched return marshalled as a raw pointer, Text rendered as address) was tracked as **T0371 and is DONE** — so this row is fully closed. Bare-name shadow leg = T0146 (DONE, fed8b940b). | P1 | T0106/T0371/T0146 all done | — | **RELATED, ROOT NAMED 2026-08-09 (T0691):** bare-name method resolution can emit a call to a SAME-NAMED method on an unrelated type. `m.lock()` on `core.sync.mutex.Mutex<Int>` emits `CALL_G DarwinMutex.lock` (seen in `verum build --emit-vbc`), which is why every `Mutex.lock`/`try_lock` fails at Tier-0 and why the async task queue, signal registry and global tracer are inert. Independently the same day: `implement<A, B> P for (A, B)` routes `(1,2).arity()` to `ArgBuilder.arity`. The constructor call on the adjacent line resolves correctly in both cases, so this is method-name resolution specifically. **LIVE INSTANCE FIXED 2026-08-09 (c8ab27455):** the four colliding platform aliases (`Mutex`/`Condvar`/`SpinLock`/`Once` in core/sys/{linux,darwin}/thread.vr) are removed — measured zero consumers, zero globs on `thread.*`; lock/try_lock false→true, sync/mutex 39/3→41/1. Census: 9 aliases stdlib-wide, 5 collided; the remaining 4 (SockaddrIn/SockaddrIn6/Stat/Thread/ThreadError) are measured LATENT — a static call on the type name does not run through `resolve_type_alias`, only a method call on a VALUE receiver does. They must not be removed by analogy (API cost, zero gain). The compiler-side cure — early return in the mount-provenance block of `resolve_type_alias` (verum_vbc/src/codegen/mod.rs:1414) — is located but gated on an alias-key census (VERUM_TRACE_ALIAS_KEYS, 7914915a3): if any alias exists only under a bare key, an unconditional early return would stop resolving it.
| A7 | ~~Bake nondeterminism (string-dice)~~ **CLOSED 2026-07-16 (T0120)**: deterministic total order `canonical_name_better` (4a667d087) + ACCEPTANCE PROVEN — two consecutive `verum stdlib precompile` runs byte-identical (sha256 `936a8375…`) on an isolated HEAD worktree. Re-export leg was split out and closed as A20. AOT-suite-count stability follows from archive identity; spot-verify opportunistically. | ~~P1~~ done | T0120 journal | ✅ byte-identical bakes proven |
| A8 | **AOT reference-return classes** — `&Text`/`Maybe<&T>` returns crash or mis-read under AOT. **2026-07-16: PARTIALLY CLOSED** — facet 1 live-green (`QueryRow.get_index → Maybe<&Text>`, 072fafab4; `Text.as_bytes` 1aad1409a); residual `[[btree_pattern_match_ref_generic_class]]` family in 10+ suites (T0109; record-OOB leg = T0147). | P1 | grep the family marker | family pins green under `--aot` |
| A9 | **FFI placeholder arms** — `GetLibrary` returns null "for now", `IsSymbolResolved` hardcodes true, 9 MachVm*/MachSem* arms emit const-zero unconditionally, CreateCallback passes through. **2026-07-16: STILL OPEN, anchors refreshed (T0110); Tier-0 twin `__fd_read_*`→0 = A28/T0111.** | P1 | `instruction.rs:28665`, `:28685`, `:29195`, `:29992` | real implementations or explicit unimplemented markers |
| A10 | **SIMD scalar-width-1 degrades** — 30+ SIMD arms lower as documented scalar fallbacks; MaskedStore/Scatter are silent no-ops (writes dropped). **2026-07-16: STILL OPEN, anchor refreshed (T0112).** | P1 | `instruction.rs:25228` family, no-op stores `:25537` | either real vector lowering or loud unimplemented |
| A11 | **Tier-0 futex** — ~~FFI gap~~ ROUTED 2026-07-12: platform futex_wait/wake wrappers now call the canonical intrinsics (InlineSequence FutexWait/Wake → Tier-0 futex_park parking table; AOT verum_futex_* shims). Raw __ulock/SYS_FUTEX/WaitOnAddress FFI left the Tier-0 hot path. **2026-07-16: suites still pinned (24 pins: barrier §3.1 ×15, condvar §3.6 ×9) — re-run + flip = T0113.** | P1 | `sync/barrier/*`, `sync/condvar/*` pins | barrier/condvar suites green under `--interp` |
| A12 | **CSPRNG intrinsic gap for `fill_secure` consumers.** ~~MOSTLY CLOSED — random_u64 registered, reservoir un-ignored~~ **2026-07-20: FULLY CLOSED (T0114 + T0144, a12de4287 + a068e0f89).** The remaining bloom/hyperloglog/count_min pins were NEVER a CSPRNG issue at all (that framing is now doubly superseded) — root-caused to a cross-module BARE-NAME CALL COLLISION: `Sha256.finalize`'s bare `compress_block(...)` call was binding at bake time to SHA-512's same-named public free fn (both arity-2; the stdlib bake has no module-scope authority for bare calls, so `lookup_function_with_arity_in_scope` degrades to a last-registration-wins global table). Same class as T0144's cross-module `Call` identity fix; T0114 landed the module-scope authority + a named loud miss. **Needs a suite re-run** to confirm the 7 pins (bloom 4, hyperloglog 1, count_min 2) actually flip — not yet observed green by this reconciliation (see `core-tests/INVENTORY.md`, still cites the old CSPRNG diagnosis). | P1→P2 | a12de4287 (T0114), a068e0f89 (T0144) | root fixed; pin flip pending a re-run |
| A13 | ~~**verum_types unreachable-pattern arms (9)** — real logic smells in inference.~~ RESOLVED 2026-07-15 (5cfb3c0e3): modules.rs duplicate `Type::Var` arm removed; expr.rs Numeric-guarded `min\|max` duplicate removed; types.rs broad `(Int\|Named, Pointer\|Volatile)` cast arm split so the sized-integer-alias restriction on the `Named` side is restored (was silently accepting `ptr as AnyStruct`). 9→0 unreachable warnings; verum_types tests 1035/0; stdlib bake green. | P2 | `infer/expr.rs`, `infer/modules.rs`, `infer/types.rs` | ✅ warnings gone via fix (not allow) |
| A14 | **Codegen panic surface** — **664 at 2026-08-12**, measured by the gate that now owns this number (`scripts/ci/check_panic_surface_ratchet.py`: recursive over `crates/verum_codegen/src/llvm`, `#[cfg(test)]` regions brace-stripped, ratchet at baseline). Historic series 501 -> 621 (07-16) -> 626 (07-19) -> 664 — still growing, and the entry's own figure had been 38 behind for three weeks. NOTE for anyone re-measuring by hand: a plain `grep -c` over `llvm/*.rs` answers 715 because it counts test regions and the gate does not — quote the gate, not the grep. **PREMISE CORRECTED by the 2026-07-19 triage (T0131)**: "malformed VBC crashes the compiler" describes only **16 sites (2%)**. The real split of 619 classified: **566 (91%) are LLVM-API returns** (builder/type/module results — an infrastructure-failure policy question, not input validation), 16 malformed-input-reachable, 3 unsupported-shape (should be diagnostics), 1 provably-infallible, 33 unclassified (mostly test code). **The mechanical sweep everyone would attempt is impossible**: 592 of 619 (95%) sit in fns returning a PLAIN value, not `Result`, so `.or_llvm_err()?` does not compile until the enclosing signature changes and every caller propagates — the unit of work is ONE FUNCTION, not one call site. **Concentration is extreme and is the good news**: the top 8 fns hold 433 of 582 production sites (74%), all `get_or_declare_*` one-shot runtime-helper IR builders. Split into claimable pieces: T0424 (ratchet — do first), T0423 (cluster C, the only input-dependent sites), T0427/T0428 (clusters A/B, mechanical). | P2 | T0131 journal (full classification + per-fn blast radius) | the ratchet is green and the count trend is reversed; cluster C converted to diagnostics |
| A15 | **Blanket `#![allow]` hides dead code** — verum_codegen/verum_verification suppress everything; ~29 confirmed-dead pub fns in `llvm/` (SIMD/bitfield/cbgr/wasm clusters). **CLOSED — verified 2026-08-12 by reading the files: `verum_codegen/src/lib.rs` and `verum_verification/src/lib.rs` carry ZERO `allow(` lines and zero `dead_code` mentions. Removed 2026-07-27 in 08470018a ('remove blanket allow(...) and clear the …'), so the entry's own '2026-07-16: unchanged (T0132)' status line had been stale for sixteen days.** Historic text follows: 2026-07-16: unchanged (T0132) — `verum_codegen/src/lib.rs:1-5`, `verum_verification/src/lib.rs:1-3,81-84`. | P2 | audit-warnings §1–2 | blanket allows removed; dead fns deleted; warning budget real |
| A16 | **Cross-compile host-default risk** — `for_host()` is the default no-libc config path; verify `--target` overrides on cross builds. **2026-07-16: anchor refreshed (T0123)** — `link.rs:573`; override path exists (`linking.rs:301` → `for_platform` `link.rs:560`) but unproven. | P2 | `verum_codegen/src/link.rs:573`; callers `verum_compiler/phases/linking.rs:244,325` | cross-build test proves target config wins |
| A17 | **`static mut` cells >8 bytes fall back to TLS; non-zero initializers need ctors** (open work inside intrinsic-contract Rule 4); CBGR bound-check `1<<24` is a stated workaround pending NaN-box tag (Task F4). **2026-07-16: unchanged (T0133)** — TLS fallback `interpreter/state.rs:658-670`; `CBGR_REF_ABS_INDEX_MAX` `state.rs:2891`. **2026-08-11 partial re-check**: the `1 << 24` ceiling is no longer the live path — it is now `LEGACY_CBGR_REF_ABS_INDEX_MAX` in `handlers/cbgr_helpers.rs`, commented "legacy `is_cbgr_ref` only" and "the window that never closed the hole", so a tagged encoding supersedes it and only the legacy decoder still consults it. The `static mut` half was NOT re-verified: `state.rs` now describes "process-wide backing cells … stable byte-addressable storage with a real address", which reads like the TLS fallback was replaced, but the >8-byte case specifically was not measured — do not close this row on that reading alone. | P2 | intrinsic-dispatch-contract.md Rules 4–5 | wide cells + initializers native; TAG_CBGR_REF lands |
| A18 | **Mono persistent-cache read side disabled — needs a portable materialiser** (2026-07-13, bfc328704): `ResolvedSpecialization::Cached` had NO consumer (merger materialises only `StdlibPrecompiled`), so every ~/.verum/cache/mono hit SWALLOWED its instantiation (pending=0, mono no-op). Read consult disabled in `mono/resolver.rs resolve_one` (write side + `validate_cache` retained). Real fix: a cached-function format that carries its string/type/function id maps so the merger can decode + remap into the target module. **2026-07-16: unchanged (T0134)** — `mono/resolver.rs:294-295`, `Cached` consumer-less at `:341-343`. | P2 | `mono/resolver.rs` Step-2 comment; memory: session_2026-07-13 | cache read re-enabled behind a materialiser; cross-module id remap proven by a differential test |
| A19 | **MEM-PTR-DEREF-TIER0-1 — Tier-0 typed-pointer deref/arith lacks the two-worlds provenance model** (2026-07-15): `ptr_read` is an identity stub / `ptr_write` a silent no-op on the interpreter (AOT correct — cross-tier incident); ptr arithmetic is Value-slot-strided (×8) for every pointee. One `*const T` fronts TWO physical layouts (NaN-boxed Value arrays vs packed bridge/Text/typed-array bytes) — static types cannot discriminate. Design recorded in session task #16: bridge-allocation interval index + PtrReadT/PtrWriteT/PtrAddT {width} sub-ops with runtime provenance branch + AOT twins. **2026-07-16: STILL OPEN (T0108)** — pins live at `intrinsics/memory/property_test.vr:187,:334`; `__ptr_read_i64`→0 `calls.rs:2543`; `DEFAULT_STRIDE=8` `expressions.rs:29193`. | P1 | `handlers/cbgr.rs:165 handle_deref`; `expressions.rs ptr_intrinsic_byte_stride` | memory property §8 pins (`law_ptr_write_read_round_trip_over_extremes`, `law_ptr_add_byte_stride_is_one`) un-ignore green BOTH tiers | **BLAST RADIUS MEASURED 2026-08-09 (T0108).** Address-of-field works on an OWNED record and fails on ANY reference — six-line repro, no stdlib: `fn poke(c: &Cell) { unsafe { *(&c.v as *const Int as *mut Int) = 42; } }` dies with `StructFieldAddr: receiver register r0 holds Some(3), expected Pointer or Int`; tag 3 is TAG_UNIT, r0 is the parameter register. Same for `&mut Cell`; the owned form writes 42. Ordinary `c.v` reads through the same reference work everywhere, so value position and address position take different lowerings and only the second loses the receiver. PRODUCTION CASUALTY: `core/sync/rwlock.vr:311` `deref_mut` is exactly this form, so **every write made through an RwLock guard is silently lost** (`{ let mut w = l.write().unwrap(); *w = 42; }` then read -> 1). `sync/rwlock` had been 28/28 green while never calling read/write; pinned in 0d46d7304. Also suspected on this radius: `Mutex.lock()`/`try_lock()` never succeed (T0691). **PARTIALLY CLOSED 2026-08-09 (bf50ad165):** StructFieldAddr now resolves its receiver through `cbgr_helpers::resolve_arg_value`, the same authority `handle_get_field` uses — both sites were measured to receive byte-identical values (`0x7ffb800022000002`, a TAG_UNIT header with a NON-ZERO payload, i.e. a CBGR register reference) and only the read path decoded it. Address-of through a reference now writes correctly; intrinsics/memory 42/8 -> 43/7 on identical files. The REMAINING half is the two-layouts problem this entry already names: turning that raw pointer into a `&T`/`&mut T` and crossing a function boundary loses the NaN-box discipline — reading through such a reference yields `0x7FF9000000000005` (the boxed Int 5) as a raw i64. `core/sync/rwlock.vr:302-313` casts const away to fake interior mutability through a shared `&RwLock<T>`, so RwLock guard writes are still lost; it needs a real interior-mutability primitive the boxing discipline knows about, which is a reference-model decision, not a handler fix. **WRITE SIDE CLOSED 2026-08-09 (b2e63982e + 6a7472b6d):** the reborrow `&mut *(cast)` returns the ADDRESS (identity fold now admits the parenthesized raw-pointer cast — the first landing matched `Cast` while the parser keeps `Paren(Cast)`, changed nothing byte-for-byte, and the wrapper was the whole story), and `compute_field_offset` normalizes `&RwLock<T>`-shaped names to the simple layout key instead of silently returning 0 — the byte offset of FIELD 0, which is how a guard write corrupted the lock's own `state` word. sync/mutex 42/0 and sync/rwlock 31/0, both GREEN, pins on the operations themselves. THE READ SIDE REMAINS and now has a measured three-row matrix (T0108): a Deref-returned raw pointer read gives identity-объект at offset 0, raw-bits-as-f64 at offset 16 in user code, and the CORRECT value through the baked generic guard — three behaviours for one operation, selected by memory world and offset. Every candidate fix is a value-model decision (slot-pointer tag, element-repr carried facts, static-type-driven deref opcodes) — the R4 contract, not another arm.
| A20 | **UMBRELLA-REEXPORT-RESOLVE-1 — wildcard re-export resolution через umbrella brace-mounts недетерминирована per run** (2026-07-15): two runs of one suite on one binary produced different E100 unbound sets (`wrapping_add`/`popcnt`/`nop` vs `saturating_add`/`rotl`/`is_nan`/`likely`); explicit re-export lists stable; `memory_fence` MIS-BINDS to a 1-arg fence. The function-name leg of the A7/METADATA-DETERMINISM family (types leg was sorted; re-export walk was not). **CLOSED 2026-07-16 (T0115, 44d8b8620)**: one-hop qualified-key probe landed (a74924270); the intrinsics/mod acceptance block is LIVE (4 wildcard tests) and green across 3 consecutive interp runs (10/10 each). Clarified: runtime/mod has no umbrella block — its @ignores are the separate STUB-STAGE-INSUITE-1 class. | ~~P1~~ done | `core-tests/intrinsics/mod/unit_test.vr` (live) | ✅ live + 3× green |
| A21 | **LITERAL-SIZED-ALIAS-COERCE-1 — bare Int literals no longer coerce to USize/ISize/Byte params of baked free fns** (2026-07-15, surfaced by the honest v20 metadata — the params previously parsed opaque and unified with anything): `ptr_add(bp, 1)` E400s, `1 as USize` passes; the arg diagnostic renders params[0]'s type while checking arg[i] (off-by-one). Coercion machinery already exists (`unify.rs is_int_coercible_named` / NUMERIC_ALIAS_MATRIX) — the metadata-fn call-arg CHECK path bypasses it. ~~**CLOSED 2026-07-15/16** (34129a636, commit-labelled "A23"): 55 signature-satisfaction casts dropped, 0 residual `as USize/ISize` across the memory suite; over-arity diag now E102 spanned+named; memory 46/46.~~ | ~~P1~~ done | 34129a636 | ✅ bare-literal calls typecheck; casts removed |
| A22 | ~~**CVT-PASSTHRU-1 — `CvtToI`/`CvtToF` lower as bit passthrough**~~ **CLOSED (T0104 Leg-B)** — verified 2026-08-11 by reading the site the entry cites: `Instruction::CvtToI` now builds a real `build_fptosi_sat_i64` (truncate toward zero, saturating, matching Rust `as`), with the comment naming the exact hazard it replaced ("copying the raw register bits reinterprets an IEEE double as an integer"). The old behaviour survives ONLY behind `VERUM_AOT_CVT_PASSTHRU_LEGACY`, whose own doc says "present ONLY as an emergency revert … the honest path is the default" — so the default is the converting path and the P0 was stale. Original text: bit passthrough at Tier-1 ("for now just pass through"): dynamic int↔float conversions return the SOURCE BITS unconverted — silent wrong numerics, cross-tier divergence (interp converts). Likely explains part of the intrinsics conversion/float AOT partials (T0153). | ~~P0~~ done | `llvm/instruction.rs:2436` | real fptosi/sitofp (or tagged dispatch); differential conversion test both tiers (T0104) |
| A24 | ~~PROOF-TACTIC-ACCEPT-1 — proof checker accepts ANY tactic~~ **CLOSED 2026-07-17 (T0105)**: the anchor (`verum_types::ProofChecker`, a second never-wired checker whose tactic arms were `Ok(())`) had ZERO callers — REMOVED per the redundancy directive (NOTE left in `verum_types/src/lib.rs`). The LIVE holes were three-fold and are all gated now: (1) `pipeline/theorem_proofs.rs` warn-only on failed proofs → **E0319 error** (unproved goals + hints, spanned); admitted (`admit`/`sorry`) proofs → **W0319 warning** on every build incl. closure-cache hits (`CachedVerdict::Ok.admitted` + verdict-schema rev in fingerprint); (2) `phase_verify` early-returned when a module had no refinement-typed FUNCTIONS, skipping Phase 4b/4c entirely for proof-only files; (3) `run_check_only` never ran the verify phase at all (`verum check` passed what `verum build` would reject) — check ⊆ build restored; `verum verify` + `verum test` preflight opt out via `VerifyMode::Runtime` (they verify separately). Admit-detection is a recursive AST authority (`ProofBody::contains_unsafe`) — the old string sniff compared lowercase "admit" to the Debug render "Admit" and never fired. Negative gate: `crates/verum_compiler/tests/theorem_proof_gate_tests.rs` (8 tests). | ~~P0~~ done | theorem_proof_gate_tests.rs | ✅ E0319/W0319 live on run+check+build; 8-test gate green |
| A25 | ~~Structural protocol conformance returns true unconditionally~~ **CLOSED 2026-07-16 (T0117, 58fd3947b)**: the lying `check_structural` API had ZERO callers workspace-wide — the threat was dormant. Per the redundancy-elimination directive the dead API was REMOVED (not speculatively implemented); a NOTE at the site sets the bar for any future structural-typing feature. | ~~P1~~ done | `verum_types/src/protocol.rs` (NOTE) | ✅ removed; no callers |
| A26 | ~~**Empty/stub fn bodies accept any return type**~~ **CLOSED for user code (T0118)** — re-read 2026-08-11: the `is_stub` guard that skipped the check for empty blocks is GONE, and `check_block` now runs, so `fn f() -> Int { }` is rejected by the one authority (an empty `{}` is Unit; Mismatch: expected Int, actual Unit). Bodyless-by-design declarations — `@intrinsic`, extern/FFI imports, protocol signatures — parse to `body: None` and never reach that arm, so they stay exempt. **Residual, narrower than the original entry and latent**: in `stdlib_single_file_mode` the call is `let _ = self.check_block(...)`, discarding the result — and the pipeline sets that mode for EVERY `core/*.vr` file. The mode's stated justification is that sibling-module types may not be loaded, which is a CROSS-module concern; an empty body against a non-Unit return is a purely LOCAL fact, so the discard is broader than its reason. Censused core/: **zero** functions today have an empty body with a non-Unit return (a first pass reported 2, both inside doc comments), so nothing is being masked right now — the gap is that a future one would pass unnoticed. | P2 | `verum_types/src/infer/modules.rs` (the `stdlib_single_file_mode` discard), `pipeline/phases_orchestration.rs:928` | a deliberately empty `-> Int` body in a core/ file is rejected during the bake |
| A27 | ~~Leaked `Placeholder` unifies with anything~~ **CLOSED 2026-07-16 (T0119, 6e984cc01)**: unification scoped to the placeholder's own forward-declared name / same-name Generic / undetermined sides; anything else is a Mismatch. Validated: verum_types 1035/0, full stdlib bake green, suites green. | ~~P2~~ done | `verum_types/src/unify.rs` | ✅ scoped + validated |
| A28 | **RESOLVED (T0111)** — Tier-0 `__fd_read_all_raw`/`__fd_read_chunk_raw` returned 0 "no data"; `Child.read_stdout` silently empty under the interpreter (Tier-0 twin of A9's fake-success class). Fixed by the host fd bridge (`handlers/process_runtime.rs` `host_fd_*` + `spawn_raw_triple`; honest `__fd_read_all_raw` arm in `handlers/calls.rs`); `sys/process_ops` + `io/buffer` T0111 pins green under `--interp`. Residuals: H3 fd-space schism (open_files ids vs raw host fds — bridged, not unified); AOT twins T0376 (`verum_fd_read_chunk` emitter) / T0377 (exec/spawn_full stubs). | P1 | `handlers/process_runtime.rs` (host_fd bridge), `handlers/calls.rs` `__fd_read_all_raw` arm | real fd reads; subprocess stdout test green under `--interp` (T0111) — MET |
| A29 | **O2-default degradation surface** (2026-08-01, perf campaign / T0322). The default AOT pipeline is now `globaldce,default<O2>` — fib38 measures **1.0x native C** (was 2.0x under `always-inline,globaldce`). The historical "O1+ SIGBUS" was NOT an LLVM bug: a duplicate switch case (`TypeId::I64` aliasing `TypeId::INT`, both 2) emitted invalid IR that crashed function passes; `globaldce`-first removes the remaining corruption, which lives only in DEAD baked bodies. **Residual debt**: (a) modules with `has_ir_issues` (arity collisions / skip-body stubs) silently degrade to the un-optimizing pipeline — each degradation is a perf cliff that must trend to ZERO (forward-stub arity replacement in `vbc_lowering.rs` already cleared the perf corpus); (b) the dead-broken-baked-bodies class that `globaldce` hides still exists in the archive (A1's const-zero kin) — bodies that would crash a pass if they were LIVE. `VERUM_FORCE_VERIFY=1` catches invalid IR at emission; not yet a CI gate. | P1 | `pipeline/native_codegen.rs` (degradation site), `VERUM_TRACE_PASSES` / `VERUM_FORCE_VERIFY` | zero `degrading '…' to 'always-inline'` lines across the AOT suite corpus; VERUM_FORCE_VERIFY green on the same corpus |
| A30 | ~~**Refinement-vs-body decided by identifier NAMES**~~ **CLOSED 2026-08-10 (T0708, 0781bf030)** — `fn f(it: Int) -> Int { it.abs() }` did not parse: the body was eaten as a return-type refinement because the disambiguation was a stack of name guesses (`it`+`.` = refinement, `self` = body, `result`+`.` = body). Legal programs were rejected AND real refinements silently downgraded. Replaced by the rule the grammar implies — the body is mandatory, so the group is a refinement iff the signature continues after it. Casts deliberately keep the token heuristic (nothing follows a cast to disambiguate). | P1 | `crates/verum_fast_parser/src/ty.rs` `brace_group_is_refinement` | probe set: body-starting-with-`it` compiles, `-> Int{it >= 0} {..}` still a refinement, `match x as Int {..};` unchanged |
| A31 | **Bridge memory: one `&unsafe T` fronts TWO layouts** (2026-08-10, T0705) — `cbgr_alloc` + `ptr_write` stored a record as ONE boxed Value while the read side walks a flat field layout; and the WRITE arm was chosen by the receiver's TAG, so the identical program stored differently depending on whether the address had been through `ptr as &unsafe T`. Read side and both write arms now route through one authority each (`record_pointer_base`, `bridge_flat_store`) with provenance from `cbgr_bridge_extents` — the fact, not the tag. Bridge blocks are HEADERLESS: the +24 object-header skip must not be applied inside a live extent. | P1 | `handlers/cbgr.rs` `bridge_flat_store`, `handlers/ffi_extended.rs`, `handlers/memory_collections.rs` | `twin.vr` reads back what it wrote; base/memory improves; `intrinsics/memory` 50/0 holds |
| A32 | **`compute_field_offset` answered "I don't know" with `0`** (2026-08-11, T0384) — the unknown-type/unknown-field fallback returned `0`, which is not a refusal but **the byte offset of field 0**. Every caller then guarded it with `(0..=u16::MAX).contains(&offset)`, a test `0` passes, so the bail those guards were written for **could never fire**: an unresolved type took the same path as a correct field-0 answer and addressed field 0. This is the corruption the in-function comment already records (`RwLock<T>`: `*guard = 42` overwrote `state`); the normalization added then only shrank the set of types reaching the fallback. Signature is now `Option<i64>` — a refusal the type checker forces all three callers to handle: the two address-of paths fall through to the general path, and `offset_of(T, f)` on an unknown type/field is a compile error instead of a plausible `0`. Closed-set-with-a-default class. **Resolved 2026-08-11 by splitting the QUESTION, not the answer.** "Where is field f in T" turned out to be two questions, and conflating them corrupts memory. The **ABI** question — where the field sits in the type's DECLARED layout, which for `@repr(C)` is the C ABI — is what `offset_of`, a raw C buffer, an FFI call and `struct_layout.vr` all mean; `abi_field_offset` answers it by READING `ffi_layouts[idx]`, the layout `generate_ffi_struct_layout` already built, rather than reimplementing C padding. The **storage** question — where the field sits in a value living in Verum's own representation — answers "one NaN-boxed slot per field" whatever the annotation says; `compute_field_offset` keeps that, and the two address-of paths keep using it. Measured, after nine probes had passed: routing all three callers through the C layout made `*(&p.b as *mut Int32) = 42` on `@repr(C) { a: Int32, b: Int32 }` write at byte 4 — inside field `a`'s slot — and `a` read back as a denormal float; `core/` holds 41 `@repr(C)` declarations. `.size`/`.alignment` DO follow the C layout for `@repr(C)`, which is measured rather than argued: all four uses in `core/` are syscall ABI (`Clone3Args.size` handed to `clone3`, `IoUringSqe.size`/`IoUringCqe.size` sizing the io_uring rings), so the old `field_count * 8` was wrong at every one. Gated on `declared_repr_c` (the DECLARATION), never on `repr_c_types`, which also collects records that merely appear in an extern signature and keep the slot representation. Landed 546d9dd87. **Residual (the `index * 8` model itself)**: a correct C-ABI layout engine already exists in the SAME file — `generate_ffi_struct_layout` (`codegen/mod.rs:11826`, "proper field alignment and padding") over `verum_type_to_ctype` + `ctype_size_align` (I8/U8/Bool 1, I16/U16 2, I32/U32/F32 4, I64/U64/F64 8, ptr 8) — but it is reachable only from `pregenerate_ffi_struct_layouts` for extern boundaries. Routed to `compute_field_offset` it yields `offset_of(TwoInts, b) == 4` and `CharAndInt.i == 4` (1 byte + 3 padding), which is exactly what `struct_layout.vr` asserts and what `index * 8` contradicts. So the flat-width decision is a re-route to an existing authority, not new engineering — sequenced with a re-bake, since baked bodies encode 8-per-field (`Shared.get` off=16). | P1 | `codegen/expressions.rs` `compute_field_offset` + 3 call sites; authority at `codegen/mod.rs:11826` | `offset_of` on a misspelt field is rejected; `intrinsics/memory` 50/0, `sync/mutex` 42/0, `mem/cap_audit` 40/0 hold |
| A34 | **An attribute read from the owner that does not have it** (2026-08-11, f60514677) — the parser puts a type declaration's attributes on `TypeDecl.attributes` (`fast_parser/decl.rs:2619`), NOT on the enclosing `Item`. Two consumers read `item.attributes` for a `ItemKind::Type` and therefore saw an EMPTY list: (a) `phases/ffi_boundary.rs::collect_repr_c_types`, so `safe_types` never grew past its 6 built-in C names and the FFI check told **42 conformance specs** to "add `@repr(C)`" to types that already had it — advice that could not work; (b) `phases/context_validation.rs:305`, same read for `@injectable`, **46 uses** across core/ and vcs/, found by census BEFORE it was hit. The working counter-example is `verum_vbc`'s `has_repr_c(&type_decl.attributes)`, which is why the same types get a real C layout there. Both now read BOTH owners. A sweep of verum_compiler/types/modules/vbc reports zero remaining sites. One fact, two owners, each consumer picking a different one. | P1 | `phases/ffi_boundary.rs`, `phases/context_validation.rs` | `struct_layout.vr` clears FFI validation (verified); no site reads a type's attributes through the Item |
| A35 | **Bare-name collisions in `core/` — measured and gated** (2026-08-11, 1966f3615 + 1f9530072). `make check-barename-collisions` is a two-sided ratchet over `core/`: rising above the baseline is a regression, and dropping below it without lowering the baseline also fails, because a baseline that drifts above reality stops measuring. **Two keys, deliberately, because conflating them overstates the reuse problem twofold**: `(name, arity)` = **614** is the RESOLUTION question (bare-name resolution must choose); `(name, arity, first-param type)` = **297** is the REUSE question (is the WORK duplicated). The gap is generic verbs — `mode_name/1` is declared in 42 modules over 42 DIFFERENT types and is not duplication at all. At the sqlite/native boundary: 84 by name, **15** by type; the task text records 113, so the surface shrank. Of the 84, **73 collide with `math/`** and the names ARE the elementary library (abs, acos, asin, atan2, ceil, cos, exp, floor, ln, log10, log2, pow, sign — 32 in total). **Precondition found by the same census**: `abs/1` is in `math/elementary.vr` AND `math/libm.vr`, `acos/1` in `elementary` AND `ieee754_deterministic` — math duplicates itself before sqlite duplicates math, so re-pointing consumers first requires deciding WHICH is canonical, a behavioural choice (determinism vs speed vs accuracy). | P2 | `scripts/ci/check_barename_collisions.py`, `core/rings.toml`, `docs/architecture/core-reference-architecture.md` §6quater | the four ratchets stay at or below 614 / 297 / 84 / 15 |
| A36 | **A script-cache hit ran ANOTHER program** (2026-08-11, 87cee9c93) — `verum run` on one file executed a different file's `main`: a two-day-old probe, not mounted, not referenced, in another directory. Stable and decided by the source bytes; reproduced in an EMPTY directory under a fresh filename. Established reversibly: cache present → wrong program; `~/.verum/script-cache` moved aside → correct; moved back → wrong again. Root: `CacheMeta::source_len`, documented as a "quick stale-detection signal independent of the digest", was WRITTEN by `store()` and never READ by `lookup()`, so a hit was accepted on the key alone — the means to check existed and was not consulted. `lookup` now verifies it and treats a mismatch as a MISS (recompiling is always correct; refusing would turn a corrupt entry into an unusable toolchain), with a warning naming both lengths. **Consequence beyond the fix**: every measurement taken through `verum run` before this was provisional — several readings from this session were retracted on those grounds. Ruled out and recorded: the JIT cache (moved aside, persisted), directory contamination, the filename, a compile failure with fallback (exit 0), and every property of the leading comment tried (colon, length, substrings). The cache key itself (`blake3(schema ++ compiler_identity ++ source ++ version ++ flags)`) is NOT proven sound — only the second line of defence is now in place. | P0 | `crates/verum_cli/src/script/{cache,context}.rs` | the corrupt cache no longer changes what runs (verified); key soundness still open |
| A37 | **`math/ieee754_deterministic` is entirely unimplemented at Tier-0** (2026-08-11) — the module declares itself "bit-for-bit reproducible FP transcendentals, **Layer: 0 (Foundation)**" and exports **42 public functions**, every one of which bodies out to an `@intrinsic("verum.libm_deterministic.…")`. There are **42 distinct such intrinsic names and ZERO of them appear anywhere in `crates/`** — not in the registry, not in the interpreter, not in the AOT lowering. Measured, not inferred: `mount core.math.ieee754_deterministic.{sqrt}; sqrt(4.0)` panics with `@intrinsic("verum.libm_deterministic.sqrt") is not implemented in this tier`. So a module the architecture places at the FOUNDATION cannot execute at all, while its two siblings do — `core.math.libm.sqrt(4.0)` and `core.intrinsics.float.sqrt(4.0)` both return 2. This is the third math module in the `abs`/`acos`/`exp` collision set (A35), and it changes what the canon decision there is about: not "which of three implementations is canonical" but "one of the three is a declaration with no implementation behind it". The panic is honest — this row is about the gap, not a silent-wrong-answer defect. | P1 | `core/math/ieee754_deterministic.vr`, `crates/verum_vbc/src/intrinsics/registry.rs` | either the 42 intrinsics are registered, or the module states that it is AOT/Tier-1 only and Tier-0 callers get a compile-time refusal rather than a runtime panic |

| A38 | **A dotted module-path call that resolves to nothing is SILENT** — `sys.darwin.libsystem.opendir(…)` (no `core.` prefix) type-checks with an unknown type, so the body cannot be compiled and the bake ships a STUB; the failure surfaces only when a program calls it. 16 sites in `core/`, and they are load-bearing: `thread.vr`'s join / yield / futex pair, `async/executor` sleep, TLS `ctx_get`, `fs.read_dir`, and **`mem/heap.vr`'s free-list key seeding — which is the open P0 whose title reads "every branch calls something that does not exist"**. The mount spelling of the same path resolves, via `is_stdlib_toplevel_path`; the call resolver never asked that authority. Stdlib side of `read_dir` repaired (ac6f88ec4); the language half is measured but HELD — it surfaces genuine use-after-move defects in 8 files and is itself not sufficient (`read_dir` then fails on a newtype-constructor sentinel, `u32::MAX/2`). | P0 | T0916, T0807 | a module-path call resolving to nothing is an error; the 16 sites run |
| A39 | **Capability types are erased below the binding** — `Store with [Read]` parses, `subtype.rs` carries the correct attenuation rule (`c2 ⊆ c1`), `unify.rs` has arms for it, four conformance specs cover the syntax — and a parameter, a reference parameter and an annotated `let` all report as the plain base type, so nothing downstream ever sees the capability. Field access on a restricted record fails with E103, and widening is not refused at all: both directions failed identically, so no program distinguished a working check from an absent one. The four existing specs pass because they name types that are never declared and never touch a value. | P1 | T0918 | attenuation compiles on a record; widening refused with a capability diagnostic; both poles pinned |
| A40 | **RESOLVED.** The affine checker was not branch-aware: consuming a value in each of two EXCLUSIVE match arms (commit on Ok, roll back on Err) was refused as a use after move. THE OPERATION ALREADY EXISTED — `AffineTracker::enter_scope` and `merge_branch`, with a comment naming this exact use — and its only caller in the workspace was a unit test. AND THE RULE WAS WRITTEN IN THE WRONG DIRECTION, which is why the dead operation went unnoticed: that test asserted the value is still AVAILABLE when consumed in one branch of two, i.e. the merge an exactly-once obligation needs, applied to the question of whether the value may still be used — which accepts a double free. Two questions need two lattices: MAY-consumed is the union of the arms (a later use is refused against it), MUST-consumed is their intersection (a `linear` obligation is satisfied by it). A `match` has TWO walkers, one per direction, exactly as `if` does, and patching only the synth one left the fix inert — the probe that reported it inert could see only that walker, and now names which one ran. | P1 | T0920 | CLOSED — 3740 verum_types tests, nine pinning the two lattices against each other, four .vr specs, and a full 2560-file core/ sweep (coverage verified by set difference) with ZERO `used after move` / `exactly once` diagnostics anywhere |
| A41 | **Half the type diagnostics carried no error code**, including the memory-safety family (`AffineViolation`, `DanglingReference`, `BorrowConflict`, `AssignWhileBorrowed`), so every gate keyed on `error<E…>` scored them zero. Fourteen already HAD a code written into the message text and were moved to `.code(…)` (aee766a92); coded is now 64 of 100. The remaining 36 have no code anywhere and assigning one is a DECISION — the scheme already drifts (`E0203`, `E3050`, `E0811` beside `E302`) and there is no registry of allocated codes, which is how `E313` came to be used twice in one session. ~~**CLOSED 2026-08-28** (T0947): the registry is completed to every emitted code AND gated — `crates/verum_error/tests/registry_covers_every_emitted_code.rs` fails the build naming the code and the file, and runs on a PR as `-p verum_error` in the pure-tier `--tests` job. The 36 are assigned. Four collisions that were only visible once the codes sat in one table were resolved: `E1001` meant both "use-after-free" (verum_cbgr) and "stage mismatch" (verum_compiler lint table); `E201` meant both "circular import" and "uninitialized variable"; `E0203` carried two meanings, one duplicating `E0205`; `E3050`/`E3060` were four-digit codes in a three-digit scheme. `verum explain` now answers for every registered code — it returned "not found" for `E400` before.~~ | ~~P2~~ done | T0947 | ✅ gated: an unregistered emitted code fails the build |
| A42 | **A one-line script costs 137 ms; one mounted stdlib module costs 1.8 s.** Measured against bun 22 ms / node 39 ms / python 314 ms. Compilation is not the cost (`Parsing: 0.1ms` cold); the cache entry for `print("hello")` is 412 KB and for a script mounting `core.io.fs` is 5019 KB, because each entry carries a private copy of the transitive stdlib closure and every run deserialises it and re-derives three maps. `VbcModule::user_function_start` is DECLARED to record where the script's own functions begin. It is not merely unproduced — it is UNPRODUCIBLE as an index: archive-derived and user functions interleave almost completely (`hello.vr`: 1979 functions, `first_user=0`, 1955 interleavings; with `core.io.fs` mounted: 22865 and 22816), so no boundary index exists and a producer written from its definition would compute `0`. The split must key on per-function ORIGIN instead, for which `archive_func_name_to_fid` is already populated during archive load; the safety condition becomes "the archive load is deterministic" — which the build already enforces by refusing an archive baked from a different `core/` tree — rather than "archive functions form a prefix". Sized by the same probe: a ONE-LINE script's module carries 1979 functions, essentially all archive-derived; the safety condition is that the reconstructed prefix must have identical ordering, checkable by an archive fingerprint. Blocks the standing directive that all project automation (400 shell + 2858 python files) moves to Verum. | P1 | T0917 | a one-line script runs under 22 ms warm, measured the same way |

| A48 | **The type-check phase self-deadlocks on a relative glob mount.** `verum check core/intrinsics/mod.vr` never returns; sampled after 17 minutes the stack ends in `RawRwLock::lock_exclusive_slow -> wait_for_readers` under `process_import`, and only one thread is doing work — so the readers it waits for are its own. `checker.process_import(…, &registry.read())` keeps the guard alive for the whole call (a temporary in argument position lives to the end of the statement) while the lazy loader inside registers what it resolved under a WRITE lock on the same registry; `parking_lot`'s RwLock is not reentrant. IT BECAME REACHABLE WHEN A DIFFERENT BUG WAS FIXED: `set_module_registry` now shares ONE handle for `module_registry` and `session_registry`, deliberately, to stop the two copies drifting as lazy loads landed in one and not the other — turning a harmless lock on two objects into a self-deadlock on one. Three call sites had the shape (`phases_orchestration.rs`, `cross_file.rs`, `audit.rs`); all three now snapshot instead of holding. Found by a full core/ sweep that stopped advancing: a slow suite is a hang until sampled. | P0 | T0926 | CLOSED — pre-fix exit 124 after 90s, post-fix 5.48s; regression test asserts by timeout and names a real file because three synthetic projects were measured and none reproduced; full 2560-file sweep completes |
| A44 | **`type linear` was parsed, registered, tracked — and never enforced.** `check_linear_consumed` produces the only `LinearNotConsumed` in the tree and had NO caller anywhere in the compiler, so the exactly-once obligation the modifier exists for did not exist. The spec that names it, `cbgr-analysis/linear_types.vr`, was `@test: parse-pass`: ten cases named for linear semantics, verifying only that the text parses — which it would have done with every rule in the file broken. Third instance of declared-not-enforced found in one day, beside capability attenuation (T0918) and the unproduced module fields (A43). Now asked at the end of each function body; parameters are exempt, because a parameter arrives by being moved in and its scope end is its destruction site — otherwise `fn close(h: Handle) { }`, the canonical consumer, would be the one thing a linear type could not have. | P1 | T0920 | CLOSED — obligation enforced at every `return` as well as at the end of the body, parameters (including `self`) exempt, spec upgraded from `parse-pass` to `typecheck-pass`, both poles pinned |
| A49 | **A capability-restricted record was not a record, and widening was accepted.** `Store with [Read]` reported `error<E103>: Cannot access field 'n' on non-record type`, so attenuation was unusable on the shape it exists for — while WIDENING, the thing the feature exists to refuse, compiled silently. There was therefore no program that told a working check from a broken one. TWO CORRECTIONS TO MY OWN EARLIER DIAGNOSIS, both from one instrument at the binding: the binding does NOT erase the capability (`CapabilityRestricted { base: Named{Store}, capabilities: {ReadOnly} }`), and `Display` does not strip it — the misleading `expected 'Int', found 'Store'` comes from INSIDE the unifier's deliberate peel, so the message names the base because the comparison at that depth is the base. And the soundness trade-off the fix was held for did not exist: E103 appeared only on FIELD ACCESS, while a widening CALL already compiled. Field access now peels the wrapper where the refinement arm already peeled its own, one arm away; `check_capability_attenuation` refuses widening with E411 naming both sets. An UNRESTRICTED value satisfies any restriction — my own probe had that labelled backwards, and refusing it would make every capability parameter unreachable from ordinary code. | P2 | T0918 | CLOSED for the language — 7 unit tests, both .vr specs un-skipped; registry views remain |
| A45 | **Generic arity is checked in one direction only.** `provided > expected` is refused; `Pair<Int>` for a two-parameter `Pair` is accepted, leaving a parameter free for inference to unify with whatever arrives first — the program does not fail, it means something else. THE FIRST ESTIMATE OF THE BLAST RADIUS WAS WRONG AND IS RECORDED HERE BECAUSE THE MISTAKE IS THE LESSON: a grep for `Result<[A-Za-z_][A-Za-z0-9_.]*>` reported 194 single-argument uses across 31 core/ files and led to the conclusion that the missing half was blocked on default type parameters. The filter matched `IoResult<Int>` — 118 of them — and every other alias ending in `Result`, each of which genuinely takes one parameter. With a word boundary the count is ZERO. A proper census (declared arity of all 690 generic types in core/, against every application) finds 24 under-applied occurrences in 7 files. Default type parameters landed and are a real feature; they were not the precondition. The refusal now carries `E407`; it was plain `error:`, invisible to any gate keyed on `error<E…>`. | P1 | T0922 | `provided != expected` lands, with the blast radius MEASURED by a full core/ sweep rather than estimated by a grep; the skipped spec and two ignored tests turn on |
| A46 | **The interior of a type declaration is not checked.** One annotation, three positions, two verdicts: `let p: Pair<Int, Text, Bool>` and `fn take(p: Pair<Int, Text, Bool>)` are both refused, `type Bad is { p: Pair<Int, Text, Bool> };` is accepted. Signatures and statements are checked; a record field is reached through neither, so whatever the checker knows about a written type — arity, kinds, meta-parameter refinements, alias saturation — it does not apply where a type says what its data IS. Found while probing A45; the arity case is the visible edge and the same hole should be probed for every other check that runs on annotations. | P1 | T0924 | the discriminating triple gives one verdict in all three positions |
| A47 | **86 of 1619 `pub fn`s in `verum_types` are called only from tests** (5.3%), measured with the same filter on both sides of the comparison — a naive single-crate reading says 111 and counts everything `verum_compiler` calls as dead. Not all are debt: constructors and accessors legitimately have no internal caller. But the list is where declared-not-enforced rules hide, and two probes into it produced A44 and A45 within the hour. The census plus deliberate falsification is the reusable pairing; the remaining names in `context_check.rs` (10), `kind_inference.rs` (9) and `protocol.rs` (8) are unprobed. | P2 | — | each name classified: used-externally / genuinely-unasked / removable |
| A43 | **Five module fields are serialised, linked and read back — and never originated.** `cfg_keys`, `framework_provenance`, `function_variants` and `user_function_start` have producers ONLY in `deserialize.rs` and `linker.rs`: the reader restores them, the linker copies them between modules, the serialiser gates a trailing record on them being non-empty — and nothing in codegen or the compiler ever creates one. A complete, faithful pipeline moving zeros, where every stage is correct in isolation. `function_variants` is the sharpest: `serialize.rs` calls it "the one with teeth: monomorphisation variant sets". `user_function_start` is documented as the stdlib/user boundary and always reads 0, i.e. "every function is the user's" — a script-cache design was written on that comment before it was checked (A42). Eight further fields (`discharged_at_seconds`, `force_inline`, `item_id`, `kernel_rule`, `per_backend_bound`, `per_backend_type`, `source_framework`, `target_framework`) have no producer at all. A declared-but-unproduced field is not automatically debt — it becomes debt when a consumer reads it or a comment promises it is filled, because then the zero is indistinguishable from an answer. | P1 | T0921 | each field classified PRODUCED / RESERVED (comment corrected) / REMOVED; a gate rejects a new field with neither |

## B. Conformance debt (core-tests / vcs)

173 `@ignore` pins across 54 files; ~90 are genuine compiler-defect
pins collapsing into the classes below; ~40–77 are the *absence of a
network test harness* (one infra item, B4); 676 audit-gap entries are
mostly API-surface polish (B5).

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| B1 | **type_id=0 field-access OOB — CLOSED 2026-07-12.** TUPLE-POSITIONAL-FIELD-1 (layer-1) + ALIAS-INTRA-MODULE-ORDER-1/Phase-2.9/mount-provenance (layer-2, alias_hit=true bakeAR witness) landed & fence-pinned. probe_udp now advances through construction + alias-static + syscall. RESIDUAL is a DIFFERENT class → see B1b. | done | fence markers | ✅ construction + dispatch correct |
| B1b | **FFI-BYTE-BUFFER-STRIDE-1 — bind/send CLOSED 2026-07-12** (bakeAW: `bind_a_ok` + `sent=4` through the embedded archive). serialize_socket_addr keeps returning List<Byte> (correct values + explicit length, no NUL-truncation — the Text-wrap attempt failed on strlen); each of the 8 socket syscall sites copies into an IN-SCOPE packed [Byte;28] and passes &sa[0] + explicit count. Pure .vr. RESIDUAL on recv: distinct bug → B1c (@ffi_name ignored). Then real loopback round-trip test replaces the placeholder @ignore stubs. | done (bind/send) | probe_udp bind_a_ok/sent=4 | ✅ bind + send |
| B1c | **@ffi_name/@link_name ignored — CLOSED 2026-07-12** (fence-markered). extract_ffi_symbol_name + ffi_symbol_link_names side-map + build_module prefers it for the dlsym key; fixes mmap/recvfrom/rename/symlink/vm_allocate. bakeAX: recvfrom now RESOLVES (no more "symbol 'recvfrom_ffi' not found"; probe_udp reaches recv). | done | codegen/mod.rs | ✅ symbol resolves |
| B1d | **UDP recv path — .vr FIX LANDED 11a6aafb6 (NO marshaller/ctype).** The ctype-driven marshaller was the WRONG frame: the packed byte-buffer representation ALREADY exists (`[Byte;N]` annotation → NewByteArray → TypeId::U8 contiguous; `container_to_slice_fat_ref` U8→reserved=1 → `.as_mut_ptr()`/Unslice yields the packed data ptr). Four .vr fixes: (1) safe_recvfrom used `transmute(buf)` on the slice VALUE (fat-ref struct bits) → `.as_mut_ptr()` (matches getsockname/getsockopt); (2) `addr_buf=[0_u8;128]` (NewList reserved=0 strided) → `[Byte;128]=[0;128]` packed, passed as subslice `&mut addr_buf[..]`; (3) deserialize_socket_addr read family@buf[0] but BSD sockaddr_in is {sin_len@0,sin_family@1} → macOS reads buf[1] (udp+tcp); (4) local_addr → live getsockname. Layer 5 (83be4c7aa): `&addr_buf[..addr_len as Int]` mis-parsed as `(&addr_buf[..addr_len]) as Int` (cast binds to the whole range) → parenthesize `[..(addr_len as Int)]` at all 11 deserialize sites (a for-loop `0..n as Int` parses correctly — slice-context quirk). EMPIRICAL RULE: packed `[Byte;N]`→`&[Byte]` param is packed ONLY via SUBSLICE `&buf[..]`; bare `&buf`/`&mut buf` does NOT coerce → **SLICE-COERCE-ARR-1 (task #24)** the footgun-killer. **VERIFIED (interp) on a clean private build: probe_udp → ROUNDTRIP_OK** (local_addr=127.0.0.1:64217 real ephemeral port, sent=4, recv=4, from=V4(127.0.0.1:61880), bytes="ping"). Contract pinned `docs/architecture/ffi-byte-buffer-contract.md` (89d95451d). AOT mirror BROKEN (getsockname ABI i64-vs-i32; sent/recv/port=0) → **task #26**; socklen write-back → #25. | **interp DONE ✓** (11a6aafb6+83be4c7aa+aab469ef0); AOT #26 | ffi-byte-buffer-contract.md; tasks #24/#25/#26 | ✅ interp ROUNDTRIP_OK || B6 | ~~INVENTORY taxonomy drift~~ **CLOSED 2026-07-16 (T0137, d054c2b13)**: website `status-convention.md` now defines `stable` (legends unified) and carries the five-statuses wording; INVENTORY's `undocumented` note (claimed unused while `context/layer` used it) is fixed. | ~~P2~~ done | d054c2b13 | ✅ one shared taxonomy, both legends updated |
| B7 | **vcs real blockers** — #143 **CLOSED 2026-07-19 (T0125, a4346829f)**: the "Waker.drop NPE" label was a stale April symptom name; the real defect was OWN-DECL-LAYOUT-EVICT-1 — `claim_user_type_name` evicted a stale layout only when a previous TypeId binding existed, but the stdlib bake seeds each module with earlier modules' layouts TypeId-free, so a foreign same-named layout owned the simple key (congruence_closure's own `Term` hijacked by `Signal.Term` ctor tag 10). congruence_closure `@skip` removed and green 7/7; aes_block_kat promoted to run-interpreter; page_roundtrip advanced to a distinct root = T0401. #76 AOT protocol-method wrong-impl dispatch (= A6 leg): **2026-07-20 update** — both named blockers are now DONE: T0242 (braced-mount standalone resolution) landed RESOLVED-SINCE (ca22a3891, no code fix needed — an unrelated change had already absorbed the root) and A6's own AOT dyn-dispatch bug is now fully closed (T0106+T0371). Neither blocker should still apply; the vcs spec has not been re-run against a post-fix binary by this reconciliation (build-free), so un-skipping is a re-run away, not a design question. | P1→P2 | `vcs/specs/L0-critical/_codegen_regressions/aot_to_socket_addrs_dispatch.vr:6` | #143 ✅; #76's two named blockers both closed — needs one confirm run to un-skip |
| B8 | **Pin census — RE-MEASURED 2026-07-19; the 2026-07-16 figures were inflated ~36%.** The old census counted `@ignore` **mentions in comment prose** as pins: 424 real attributes + 235 comment-only mentions = 659 ≈ the "670" that was reported. **Truth by attribute** (excluding lines whose first non-space chars are `//`): **424** total; io 32 (was "48" — T0146's own root-cause independently found 32, class proper 21, 11 flipping on the dispatch fix alone), meta 31 (the "57" static-dispatch class was really 13 — **T0150 CLOSED 2026-07-19**, fixed-since by DELIM-FANOUT-SQUAT-1 8bf486ddc), collections 60 across ALL classes (was "90" for one class; btree itself is 8 — see T0147), net 89 of which only **11 are defect pins and 78 are legitimately harness-gated** (proxy/websocket/unix/url — T0149). Remaining per-class owners: T0146 (DONE — io dispatch), T0147, T0148 (base — its own suites re-measured far WORSE than census: panic 27F→114F, env 4F→94F, iterator 11F→282F), T0149. **T0151 CLOSED 2026-07-17 (2d8dd42f0)** — its "range-iterator dispatch" framing was a misdiagnosis for 14 of 18 pins; the real root was f-string literal segments never getting escape-decoded (front-end, tier-coherent), healing 121 `core/` f-string-escape sites. Also the AOT tier-partials T0152/T0153/T0154 (whose "35F/39F" are FAILURE counts from AOT runs, not pin counts — those suites carry 0 pins). **Any headline "N-pin" figure must be re-measured by attribute before use**; a mechanical gate for this is specified in T0220. | P1 | measured at HEAD 2026-07-19; per-suite method in T0220 | per-class tasks close; census re-run by ATTRIBUTE trends to zero |
| B9 | **Masked-red HEAD test targets (cargo fail-fast chains)** — a red test TARGET hides every alphabetically-later target. Chain unmasked so far: coercion-marker drift (CLOSED T0168/58f5519af) → `dependent_types_comprehensive` trio (CLOSED T0169/dfb47e4b1) → `integration::test_occurs_check` (CLOSED T0171/4bddeaa8d — #115 deliberately breaks Function cycles; re-pinned on a tuple cycle + companion) → `refinement_integration_tests::test_check_with_evidence_stronger` (OPEN **T0174**, 27/1). Clean-HEAD compile broke TWICE the same day: T0167/01dbb1024 (ConstValue arm) and **T0173 (CLOSED 2026-07-16, 8f82d9f16 — fixed the same day it was filed; the `InlineSequenceId::Env*Seq` variants were committed).** The only remaining open item on this row is **T0174** (refinement_integration_tests::test_check_with_evidence_stronger, 27/1). A nightly clean-worktree build + `cargo test --no-fail-fast` sweep would surface the whole set at once. | P1 | T0167-T0174 | clean-tree build+test sweep green or fully pinned |
| B10 | ~~**`static_assert` asserts NOTHING**~~ **CLOSED 2026-08-11 (T0384)** — 546d9dd87. The construct was declared in `infer/env.rs` and implemented in neither tier: 154 calls type-checked and asserted nothing, while anything reaching codegen died on "undefined function". Now folded at compile time — true discharges silently, false is an error carrying the assertion's message, and **unevaluable is also an error** (an assertion the compiler could not check has not been honoured). A failed assertion halts the build in EVERY mode via the new `SkipClass::Fatal`, because codegen's default mode is lenient and would otherwise have dropped the function with a warning — trading one false green for a more convincing one. Accepts one or two arguments: the corpus writes `static_assert(cond, "why")` while the environment declared that form under `static_assert_msg`, which nothing writes. Sequenced AFTER the `@repr(C)` layout fix (A32) so the corpus's assertions are TRUE when enforcement arrives. **Residual, deliberately scoped**: named constants (`SHADOW_SPACE_SIZE == 32`, 2 uses) and `size_of_ptr()` (1 use) do not fold — codegen has no name→value map (`ctx.constants` is a value POOL; `pending_constants` is drained before bodies compile) — so those files report "cannot be evaluated" instead of compiling. Not a regression: they failed with "undefined function" before. The other five declared-but-unimplemented builtins (`static_assert_msg`, `debug_assert_msg`, `writeln`, `watch_channel`, `properties_of`) remain — `properties_of` is the notable one, being the only `.vr`-level evidence for Computational Properties. | P1 | `crates/verum_vbc/src/codegen/expressions.rs` (const_eval + arm), `codegen/error.rs` (`SkipClass::Fatal`) | a deliberately false `static_assert` fails the build in DEFAULT mode; struct_layout.vr's C-layout assertions hold |
| B11 | **CI red since the gate that introduced it (2026-08-08) — three distinct causes, measured, not one** (2026-08-11). `gh run list --workflow=CI` shows failure from the run titled *"ci: INVENTORY structural integrity becomes a gate"* onward. (a) **INVENTORY gate** — `no_new_duplicate_module_rows_in_inventory` failed on `sys/mod: 2 rows`: the duplicate the gate exists to prevent was already in the file when it landed. A 2026-08-08 census added a `sys/mod` row believing none existed; one did, spelled `` `sys/mod` (umbrella) ``, and the suffix hid it (differently-spelled-key drift). FIXED here — rows merged, newer measurement governs (39 compile-error / 0 passing, counts zeroed, 2026-05-27 figures kept as history), and `KNOWN_DUPLICATE_ROWS` emptied: all 17 entries were frozen at `1`, which is the NON-duplicate case, so they excused nothing and could never go stale — an inert list that read as an allowance list. (b) **Unit tests (arm)** — exit 143, *"the runner has received a shutdown signal"*, after ~22 min of compile + stdlib bake; no test ever ran. The bake accounts for ~19 of those 22 minutes (a single 17:20→17:40 gap in the log). It is NOT an unconditional cost: measuring the largest inter-line gap in two other jobs of the same run gives 0.7 min and 0.1 min, so those jobs did not bake — i.e. this is a cache MISS on that job's `shared-key: unit-<os>` (rust-cache is configured), and the exposure is that one cold cache on this job exceeds the runner's budget. (Caveat on the measurement: gaps were computed over the first 400 timestamped lines per job.) The standing stdlib-delivery-speed directive makes the bake's cost in CI worth attacking directly, but the fix is cache scope, not "the build is slow". (c) **VCS L0 + L1 (100% gate)** — `RESULT: FAILED (45 failures)`, `L0: 610/656 (93.0%)`. Of those 45, **30 are TIMEOUTS** (28 at ~5000 ms, 2 at 30000 ms) and only **15 are genuine fast failures** (~700-800 ms) — so two-thirds of that gate's red is a time budget, not 45 broken features, and triaging it as 45 defects would be wrong work. | P1 | `crates/verum_vbc/tests/inventory_module_rows_unique.rs`, `core-tests/INVENTORY.md`, `.github/workflows/ci.yml` | INVENTORY gate green; (b) and (c) re-measured before any triage |
| A33 | **A diagnostic can lose its source location — silently** (2026-08-11, T0384). Measured: `error<E400>: Type mismatch: expected 'Int', found 'Bool'` prints `--> file.vr:2:18` for a two-line program and prints **no location line at all** for `vcs/specs/L3-extended/ffi/c_interop/struct_layout.vr` (461 lines). Same error code, same renderer, different producer. `TypeError::Mismatch` carries a `span`, so the loss is between construction and the Diagnostic's primary label. `RichRenderer::render_location_header` rendered the header only `if let Some(span) = diagnostic.primary_span()` **with no else**, so "location lost" was indistinguishable from "location not needed" — the reader was left to search the file by hand, which is what made this survive. Renderer now prints `<no source location attached to this diagnostic>` instead: it invents no position, it makes the producer's failure visible and greppable. **RESOLVED 2026-08-11 (cffb33cbb + the W_NOEFFECT leg)** — 20 of the 24 positionless builders now carry a span. Root was not a lost span but an ABSENT one: `DiagnosticBuilder::warning().message(..).build()` with no span call, inside `check_cast`, whose signature takes `span: Span` as a PARAMETER — 15 of the 24 sat in that one function. The rest: 2 in `infer_expr_throw_expr` (`expr.span`), 2 in `check_function_inner` (`func.span`), 1 W_NOEFFECT (`stmt.span`). Measured on the corpus that surfaced it: 4 positionless diagnostics before, 1 after, and the cast warnings now read `struct_layout.vr:380:9`. The type system caught the first attempt — the builder wants `LineColSpan` and the checker carries byte-offset `Span`; `span_to_line_col` was already imported in the same file. **Remaining 4, each with its reason rather than a plan**: `TypeError::Other` (**re-measured 2026-08-11: 336** constructions in verum_types — **and the means to fix them ALREADY EXISTS**, which changes the work entirely. `TypeError` carries three siblings: `Other(Text)` (no code, no span) used **336** times, `OtherWithCode { code, msg }` **6** times, and `OtherWithCodeSpanned { code, msg, span }` — the complete form — **10** times. The best option is used 10 times and the worst 336: not a missing capability, a path-of-least-resistance problem. Recorded from experience, because it is the session's recurring shape landing in my own work: I began adding a FOURTH variant before the compiler's exhaustiveness check pointed me at the existing siblings eighty lines up the same enum. Reverted; three call sites now use `OtherWithCodeSpanned` with `E406`. No new type is needed — the rest is per-site, and **35 of them sit in scopes that already hold a span** — `expr.span`/`stmt.span`/`func.span`, 33 in `infer/expr.rs` alone. Those 35 are a per-site RE-TARGETING onto the existing `TypeError::LanguageLaw { code, message, help, span }` variant, NOT a variant widening; the other 329 have no span in scope and are the campaign-scale part. First one landed: the explicit-type-argument arity error now raises `LanguageLaw` with `E408` plus a help line — **and it exposed the NEXT layer**: the marker is gone and the file is named, but the position renders `1:1` (file start) rather than the call site, because `expr.span` is a DEFAULT at that point. Controlled: a `let x: Int = true;` on line 6 of a blank-padded file reports `6:18`, so spans work in general; the earlier `E100: unbound variable: ln` also reported `1:1`. **Localised further, and the wall is narrower than feared**: an ordinary generic function reports correctly — `ident<Int, Int>(5)` on line 6 of a padded file gives `span_generic.vr:6:13`. The `1:1` case is `bits_of`, declared `public meta fn`, i.e. the META path. The parser does set a real span on a call (`lhs_span.merge(self.stream.make_span(start_pos))` in fast_parser/expr.rs), so it is lost after parsing, and `crates/verum_compiler/src/meta/` contains **99** `Span::default()` / `Span::dummy()` occurrences — synthesised meta nodes carry no origin. So the retarget IS sufficient for ordinary code; only meta-expanded expressions land at 1:1, and fixing those means carrying the call site through meta expansion — `E402` was checked against the registry and is taken by "Send bound not satisfied", and a code is a name, so reusing one would create a second meaning under it) and `RecursionLimit` have NO span FIELD, so localising them means widening the variant across every construction site — campaign-scale, not a follow-up; `W_STDLIB_SHADOW` is raised by `glob_shadow_arbiter(name, provenance)`, which genuinely has no position to give. The one E400 still positionless in the corpus reaches `convert_span` with a span it cannot map; the release backtrace is symbol-stripped, so localising it needs a debug build. Old sizing follows. **Residual, now sized**: `verum_types/src` contains **725** non-test uses of `Span::default()` / `Span::dummy()` / `dummy_span()`, and of the 244 `TypeError::Mismatch` constructions 123 pass `span` by field shorthand (so their quality depends on whatever the enclosing scope bound) — i.e. a diagnostic can be born positionless along hundreds of paths. Do NOT sweep all 725: the renderer marker added here makes the ones that actually reach a user visible, so the targeted procedure is (1) compile the conformance corpus, (2) collect every diagnostic printing `<no source location attached to this diagnostic>`, (3) fix only those producers. The `struct_layout.vr` E400 persists with its `const _: () = { static_assert(..) };` block commented out, so that construct is measured NOT to be the cause. | P1 | `crates/verum_diagnostics/src/rich_renderer.rs` `render_location_header`; producers in `verum_types` | no `<no source location…>` line on any spec in the conformance corpus |

## C. Stdlib (.vr) structure

Scope measured: 2546 .vr files / 581K LOC; 5820 `public type` decls;
8696 mounts; 1374 `@intrinsic` occurrences. Full enumerations +
reproducible scripts: audit scratchpad (cat1_dups.txt, cat2_drift.txt,
cat4_unresolved.txt).

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| C1 | **Duplicate public type names** — 155 names declared in ≥2 files (326 decls); 64 kind-divergent genuine collisions, of which **27 are protocol-vs-record hard collisions**. **RESOLVED 2026-07-11 (26 fixed + 1 assessed-by-design)** (batches 1-4: sqlite-internal Capability family unified, Capability family unified onto database/common; CommitDecision/AuthDecision/UpdateOp/ProgressDecision deduped to their authorities; TraceEvent→TraceEventKind, BusyDecision→BusyHookDecision (ms-vs-ns unit honesty), TraceCtx→TraceHookCtx). verify-kernel cluster ASSESSED, no action: kernel_v0 (bootstrap .vr kernel formalization) and kernel_soundness (propositional carrier for theorems about the Rust proof kernel) are two INDEPENDENT formalizations whose shared vocabulary (Judgment/Context/Derivation) is domain-canonical in each; verify/mod.vr already isolates both from flat re-export (qualified-path access only, documented in-file), so the bare-name first-wins surface is designed out. Renaming formal-methods vocabulary would damage domain honesty for no dispatch benefit. (InclusionProof resolved: merkle keeps bare; Rekor/Signed wire names; wire-shape merge tracked as follow-up.) **2026-07-16 RE-SCAN (agent-5)**: 130 duplicate names remain (was 155); **53 kind-divergent** (22 protocol-vs-data dispatch hazards — full table in the agent report/T0156; 30 data-vs-data; 1 intentional `@cfg` alias SockaddrIn); 62 same-kind twins + 15 domain-canonical formal-methods names (safely qualified, zero wildcard re-exports — no action). Top-10 shape-verified twins → T0155; 22 hazards → T0156. **2026-07-17: 22-hazard leg CLOSED (T0156, 99989c7a5)** — 12 data-side renames (GpuStream/Fts5Index/OriginField/AdvanceableClock/StmtEvictability/BuiltinAggregateState/CliHandler/K8sService/OwlLiteral/CacheSizeInterpretation/ModuleShape/SqliteConnection) + RuntimeConfig alias deleted; formal-vocab (Layer/Not/Site/Universe/Capability×4) no-action (domain-canonical, qualified-isolated); bake green 585/48418. T0155 twins still open. | P1 | 2026-07-16 scan (session 187337bc) | ~~22 protocol-vs-data hazards renamed/qualified (T0156)~~ ✅ done; top-10 twins unified (T0155) |
| C2 | ~~Mount drift~~ — **CLOSED 2026-07-11** (commit 7eca17515): 44 broken mounts → 0, detector re-run clean, bake-verified. | ✅ | cat2_drift.txt | done |
| C3 | **Unregistered `@intrinsic` calls** — ~~silent LoadNil~~ **MISS NOW TRAPS (2026-08-08, 16e93466c)**: the miss arm emits `Instruction::Panic` naming the intrinsic and the calling function, then LoadNil only for register bookkeeping. The compile-through property that motivated the nil is preserved exactly — an unreached call site still costs nothing, full stdlib re-bake green — while a LIVE call now fails instead of computing with nil. Re-measured across ALL of core/ the same day: **870 of 1233 distinct names (70%) exist nowhere in `crates/`**, and the asymmetric surface (ed25519 297 lines / p256 331 / x25519 268 / rsa 286 / ml_kem 193 / bls12_381 38 intrinsics) is thin wrappers over names that do not exist, with NO reference implementation to fall back on — so `Ed25519.sign` returned nil and looked like it worked. Three consumers fixed at the source in 930d16bbf: blake3's `wrapping_add`/`rotate_right` rewritten in Verum (correct only because T0611 made `+`/`<<` wrap at the declared width), `compress` routed to the `compress_reference` that already existed twenty lines below, `finalize_xof` made to panic instead of leaving an output buffer at its allocation zeros (an XOF feeds derive_key). Argon2id likewise returns `UnsupportedBackend` rather than a nil tag. Historical detail below. — 564 distinct names / 578 occurrences; miss = silent LoadNil (`expressions.rs:26463`) or Unit fallthrough. PROGRESS 2026-07-11 (commit 968f09f80): the LIVE silent-nil kernel rerouted to registered implementations — llvm.*.with.overflow ×6 (checked_add_u/checked_sub_u destructured nil tuples!), sincos ×3 (composed sin+cos), verum.float bit-casts ×5, verum.rng.fill_secure ×2 (→ canonical rng helper), mysql float_to_bits ×2. REMAINING: (a-residue) GPU set / SIMD compares / checked_rem/shl/shr / overflowing_neg/shl/shr / widening_mul / carrying_add / transmute / ptr_read_* / cpu_feature — need registry entries + Tier-0 handlers (Rust); (c) ~250 `verum.<host>.*` deliberate host-subsystem stubs (tls/quic/h3/k8s/xds/compress/cloud) — separate not-yet-implemented track. **2026-07-16: miss path now WARNs at bake (`expressions.rs:28807`) but still LoadNil at runtime (`:28813`); (a)-residue registry count still 0 → T0116.** | P1(a)/P2(c) | `codegen/expressions.rs:28807` | (a) registered with handlers; runtime miss loud |
| C4 | ~~Dead/drifted surface~~ — **CLOSED 2026-07-11** (f15dae9e9): AllocHandle/JoinHandleOpaque intrinsics-side alias decls deleted (Raw* canonical); `contracts_old` re-diagnosed as FALSE POSITIVE — it is the live 'contracts with old() pre-state refs' surface, retained. | ✅ | — | done |
| C5 | **Toolchain-constraint compliance** — 18 documented .vr-breaking classes. **2026-07-16 SWEEP + PROBE (T0157, 40a41bc13)**: `::`/relative-statics in live code = 0; wide literals = 16 intentional consts; **compound index-assignment constraint is DEAD at Tier-0** — all 6 shapes from the 1115-site scan green under `--interp` (`repro/index_assign_probe.vr`). Tier-1: 5/6 green; the packed `[Byte;N]` shape fails natively → split out as **T0172** (`repro/packed_byte_index_assign_aot.vr`). Remaining C5 residue: verify the other documented classes opportunistically as suites touch them. | P2 | `repro/index_assign_probe.vr` | ✅ index-assign verdict delivered; T0172 owns the Tier-1 leg |
| C6 | **Byte-conversion fn-form intrinsics return non-containers** — `@intrinsic("to_le_bytes"/"to_ne_bytes"/"to_be_bytes")` (fn-forms in `core/intrinsics/conversion.vr`, declared `-> [Byte; N]`) lower to a raw i64 reinterpret (`InlineSequenceWithWidth(ToLeBytes, 8)` → `emit_to_le_bytes` = plain `Mov`, `intrinsics/codegen.rs:2530`; MLIR leg = `arith.bitcast`, `intrinsics/lowering.rs:1385`). Any consumer treating the result as a container (`bytes.len()`, `bytes[i]`, slice-typed arg positions) gets garbage on BOTH tiers. Discovered 2026-07-13 as the root of #44-B (`Hasher.write_int` default fed a raw Int into `write`; the DefaultHasher intercept silently hashed nothing → equal hashes). Hasher path detached from it (per-byte `write_byte` chain); METHOD-forms (`n.to_le_bytes()`) are correct (interp allocs a real list, `method_dispatch.rs:4835`). Fix = container-honest lowering: dedicated instruction (RandomU64 pattern: TensorExt sub-op + Tier-0 handler + LLVM lowering) allocating a real `[Byte; N]`, or retire the fn-forms onto the method-forms. **2026-07-20: CLOSED as a STALE-CLOSE (T0130).** The cited raw-Mov site (`intrinsics/codegen.rs:2547`) turned out to be ZERO-CALLER dead code — the parallel strategy executor that owned it was deleted wholesale by an unrelated commit (T0195, 710c4c6b2). The LIVE fn-form path (`codegen/expressions.rs:29727` `InlineSequenceWithWidth`) already emits a real `NewList` + per-byte shift/mask/push on both tiers: interp conversion suite 60/60 (0 `@ignore`), AOT closed separately by T0153 (b494ddc169). No fix needed at the cited site; the row's premise no longer describes live code. | ~~P2~~ done | T0130/T0153/T0195 | ✅ both tiers already produce real byte containers |

| C7 | **Algebra-tower consolidation (T0499) — LANDED 2026-08-01** with named residuals. ONE operator-wired + axiom-bearing tower in `core/math/algebra.vr` (Semiring→Ring→CommutativeRing→IntegralDomain→Field, StarRing, Normed<R>, RealField extends Normed<Self>; laws are executable `@verify(formal)` predicates over the operator surface; Module→LeftModule); linalg god-`Numeric` DELETED, containers unbounded, ops bound on the weakest tier (`adjoint`/`dot_conj` NEW); `Complex<Float>` is a first-class Field+StarRing+Normed<Float> scalar — acceptance `vcs/specs/L2-standard/math/linalg/complex_matmul.vr` PASSES at Tier-0. Engine legs landed: operator-dispatch strip fix, arith OBJECT arm (`object_dispatch.rs`), literal witness derivation, annotation/enclosing-impl/funnel witness chain, builtin List `.clone()` spine copy. Wire-drift CLASS fixed+pinned (tensor/nn/distributed op enums; `Mean` ran Prod, `Sqrt` ran Relu pre-fix; TensorUnaryOp +15 ops; TENSOR_ARGMIN registered). Folded siblings closed at source: T0501/T0503/T0510/T0517/T0526/T0527/T0614. **RESIDUALS** (updated 2026-08-01 after the follow-through wave): ~~(2) container clones~~ CLOSED — Map/Set/Deque/List share ONE spine-clone authority; ~~(5) module-decl drift~~ CLOSED — all 232 normalized. STILL OPEN: (1) AOT parity for the generic-user-type arithmetic object arm (interp-only — documented in `intrinsic-dispatch-contract.md`); (3) assignment-form (`x = Matrix.zeros(…)`) annotation witnesses not staged (let-form only); (4) checker does not normalize `Self.Output` for method-form `a.add(b)` under `T: Add<Output = T>` (operator form fine); (6) `core.math.*` submodules unreachable by user-code mounts against the embedded archive (E402; source-path resolution works — metadata gap); (7) NEW from T0661's follow-through: `core/configuration` registry is a shell — `const BUILTIN_REGISTRY: Lazy<…>` materializes nil in the bake AND its closure's 12 format-adapter registrations are commented out; the module exports parse verbs over a registry nothing populates. | P1 | T0499 journal; probes in session scratchpad | residuals get their own rows/tasks when claimed |

| C8 | **The PRELUDE resolves 14 of its 20 math functions to SQLite's SQL builtins** (measured 2026-08-11 through `mount core.prelude.*`): sin cos tan asin acos atan exp log10 log2 sqrt pow abs floor ceil each render `Relaxed` — variant 0 of an unrelated type — instead of a number; log cbrt round min max clamp are correct. The winner is decided by (NAME, ARITY) first-wins: SQLite's `log(v, base_opt)` and `round(v, digits)` take two parameters, so those two names do not collide and math wins. DIRECT mounts are unaffected (`mount core.math.{sqrt}` -> 4.0), so the live surface is the prelude alone, not the 113-collision figure in C-adjacent notes. Bake-side witness: the precompile pass reports `canonicalised 2304 re-export leaves; 2678 left unresolved`, head of list `core.math.E, core.math.INFINITY`. **LARGELY CLOSED 2026-08-12 (T0148, 328fbf309)** — the named form (`mount core.prelude.{sqrt}`) now answers correctly for ALL 20: sin=0 cos=1 tan=0 asin=0 acos=0 atan=0 exp=1 log=0 log10=0 log2=0 sqrt=4 cbrt=2 pow=8 abs=4 floor=2 ceil=3 round=3 min=3 max=7 clamp=5.0. RESIDUAL, and it is an OVERLOAD question rather than a carry bug: the GLOB form (`mount core.prelude.*`) reaches 18 of 20 — `pow` and `abs` still resolve to SQLite because an earlier binding already occupies the name (`VERUM_TRACE_MOUNTGATE` reports `env_occupied=true ambient=false` for both, `env_occupied=false` for the eighteen that were fixed). That occupant is NOT wrong: for `abs` it is the integer-capable binding that 27 files under core-tests/ and vcs/specs/ exercise with integer literals, two of them L0-critical (`assert_eq(abs(-9), 9)`, `assert_eq(abs(-5), 5)`). Forcing the prelude's `abs(x: Float) -> Float{>= 0.0}` to win was tried and REVERTED — it made every one of those calls die on a runtime refinement violation. Serving both needs overload resolution over the bare-name surface, which is a language decision, not a resolution-priority tweak. ROOT (corrected 2026-08-11, two instruments): name resolution is NOT the blocker. `VERUM_TRACE_TASK20` shows verum_types resolving the prelude leaf transitively to `core.math.elementary.sqrt` with `functions_has=true`; `VERUM_TRACE_CALLS` shows the interpreter then calling `core.database.sqlite.native.builtins.math_fns.sqrt` (func_id 13848). The type checker's resolved callee is DISCARDED by VBC codegen's bare-name call binding — same two-resolvers shape as T0718's `min`. An earlier reading of this entry blamed the bake-vs-loader split (T0691); that was wrong and is retracted. | P1 | `scripts/ci/check_barename_collisions.py --scope prelude` (frozen at 26); T0538 / T0691 journals | `mount core.prelude.*` answers a number for all 20; the prelude scope drops below 26 in the commit that earns it |

| C9 | **Three math CONTRACTS share one flat bare-name space** — measured 2026-08-11: of 97 public (name, arity) pairs across core/math's scalar files, 31 are declared more than once; 18 of them exist in ALL THREE of `elementary.vr` (Layer 1, high precision), `libm.vr` (Layer 0, pure-Verum zero-libc) and `ieee754_deterministic.vr` (Layer 0, correctly-rounded, selected by `@deterministic_fp`): acos asin atan atan2 cbrt cos cosh exp expm1 hypot log10 log2 pow sin sinh sqrt tan tanh. **DO NOT 'DEDUPLICATE' THESE** — they are three deliberate contracts with different guarantees, and collapsing them would silently change rounding behaviour under `@deterministic_fp`. The defect is that a caller cannot SAY which contract it wants through a bare name, so resolution order picks one invisibly; the language already has the selector (`@deterministic_fp`) and the flat name space undermines it. Latent, not a measured live misdispatch inside core.math — all three answer 4.0 for `sqrt(16.0)`. | P2 | `check_barename_collisions.py --typed` (297 frozen); core/math/{elementary,libm,ieee754_deterministic}.vr headers | a caller can select a contract explicitly; the 18 shared names stop being resolvable by order alone |

| C10 | **A generic SUM type unified with ANY instantiation of itself** — `Maybe<Text>` satisfied `Maybe<Int>`, `Result<Text,Text>` satisfied `Result<Int,Text>`, and so did a USER-DEFINED `type Own<T> is Nothing | Just(T)`, while `List<Text>` vs `List<Int>` was correctly rejected throughout. Three measured severities, ascending in quietness: a panic for record payloads (that was the l1_pager defect, T0401); a silently wrong number for scalars (43 -> 49753132273); and for a foreign payload a silently DIFFERENT OPERATION — an `Int`-declared binding holding `Text` made `v + 1` string-concatenate to `привет1`. **FIXED 2026-08-12 (T0722, b4b88d8fa)** at two sites that had to be corrected together: the `unify.rs` subset branch discarded payload errors, and `synth_and_check`'s Err arm accepted two Variant types on their variant NAME SETS alone. Verified by rebaking the whole 2558-file stdlib under the stricter checker (0 errors) — a tightening that broke the library would have been one defect traded for another. **FIXED 2026-08-12 (47f62d10a)**: that arm is gone, with the whole chain it fed. It was justified as smoothing index/slice inference; measured with a positive control inside each run, legitimate `xs[0]` / `xs.len()` never reached it and neither did a 2558-file stdlib bake — only the wrong annotation did. `let y: Int = xs;` is now E400. **CLOSED 2026-08-13** — the family took five sites, and the last two were not where the register said to look. Site 4 (`d3eca0f86`): a `Result`/`Maybe`-shaped SUM unified with `Int` unconditionally, an arm scoped in its comment to "FFI wrappers" that a full stdlib bake reached ZERO times — removing it left a BYTE-IDENTICAL archive. Site 5 (`adfcfb6e5`): the acceptance was never in the compiler at all — `core/base/maybe.vr` DECLARED `implement<T> IntCoercible for Maybe<T>`, and `List`/`Range`/`RangeInclusive` declared the same, all citing the index-coercion premise this task measured false three times. The coercion-marker inventory claimed these markers "must only be implemented by the types listed here" while all four sat outside it — the coverage claim was the hiding place. Both RED specs are green; the surviving pins are `indexable_collection_not_int.vr` and `maybe_double_wrap_silent.vr` (now typecheck-fail). | P1 | T0722 journal; `VERUM_TRACE_UNIFY`; vcs/specs/L0-critical/_codegen_regressions/maybe_double_wrap_silent.vr (RED on purpose) | the residual arm distinguishes an index-inference comparison from a user's mismatched annotation; the RED spec goes green |

| C11 | **User code cannot construct a stdlib RECORD against the shipped archive.** A/B on one file, differing only by `VERUM_STDLIB_PATH`: `RsaPublicKey { n, e }` and `TableStats { ... }` fail from the embedded archive with `E400: field 'n' ... expected 'Unit'`, and compile from source; the generic `Edge { src, tgt, label }` fails differently — `expected 'L'`, the type parameter unsubstituted — and also works from source. So concrete records lose field types to `Unit`, generic ones keep them uninstantiated. Narrowed to three candidate sites: `type_descriptor_to_type` drops `Record { .. }` fields in BOTH branches; the field map is built elsewhere (`infer/core.rs` ~1415) and guards an empty `f.ty` with a fresh var; `parse_descriptor_type_string` returns `Type::Unit` for both `""` and `"()"`. **FIXED 2026-08-13 (e3da43a8d)** and it was none of the three: the archive holds SEVERAL descriptors per type name — the declaring module's, with real field types, and one per CONSUMING module, whose fields all carry `FieldDescriptor::default()` (`type_ref = Concrete(TypeId::UNIT)`). Both are `Record`, the collision policy ranked by KIND alone, and sorted-walk order picked the winner. Localised by dumping the RAW archive at test time after three code-read hypotheses failed. Gate: `stdlib_record_field_types_survive_the_archive`; catalogued as §58. **RESIDUAL ALSO FIXED (9210b6e89)**: generic records failed with the type parameter unsubstituted (`Edge { src, tgt, label }` -> `expected 'L'`) for an unrelated reason — `__type_params_<name>`, the key the record-literal checker substitutes through, was registered INSIDE `ensure_stdlib_type_loaded`'s "type not yet loaded" guard, so a type already in ctx short-circuited the helper and its params were never written. The type being loaded and its parameters being loaded are two facts; one guard cannot stand for both. **C11 CLOSED**: both concrete and generic stdlib records now construct from user code against the shipped archive. | P0 | T0724 journal with the three reproductions | archive and source agree, and a stdlib record can be constructed from user code against the shipped archive |

| C12 | **A `meta` (compile-time value) parameter is not delivered into the body.** Nine stdlib functions ship as panic stubs for this one gap — `MambaBlock.new/.forward/.selective_scan`, `StateSpaceKernel.discretize/.hippo_init`, `S4Layer.dplr_init`, `Expert.new`, `MeshTopology.new`, `ActorMesh.spawn`, plus `ClosedSubspaceLattice.top/.bottom` and `EpistemicHilbertSpace.canonical` — each with `undefined variable: N` / `H` / `k` / `DIMS`. TWO SYMPTOMS, and the second is worse: in a minimal case the read is SILENT. `implement<N: meta USize> Buf<N> { fn cap() -> Int { N as Int } }` then `Buf<4>.cap()` prints `()`, not 4 — a Unit with no diagnostic. The declarations parse and typecheck fine, refinement included (`N: meta USize{N > 0}`); what is missing is the VALUE reaching the body, and presumably monomorphisation over it. | P1 | T0726 journal with the minimal reproduction | the minimal case prints 4, and the nine functions leave the bake's skip list |

| C13 | **A check that silently stopped applying below a directory depth.** `E600: circular constant dependency` never fired when the file sat two or more directories under the cog manifest — and the spec covering it read GREEN for four levels. A constant is keyed by its FULL module path while the imported-constant edge was built from the FIRST SEGMENT of that path; the two agree only when the path has one segment, i.e. only beside the manifest. An earlier fix (T0587) had replaced a hardcoded `cog.` with the first segment — repairing the case in front of it and leaving the general one broken. **FIXED 2026-08-14 (33e00587a)**: the edge uses the ENCLOSING path, which is what "sibling module under the same parent" means, and reduces to the old behaviour at depth 1. Found by bisecting on the file's SURROUNDINGS with byte-identical copies, then probing both sides of the comparison the detector performs. | P1 | T0727 journal with the bisection table | verified at depths 1, 2, 3 and in place |

## D. Documentation

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| D1 | ~~Website policy violations~~ **CLOSED 2026-07-16 (T0135, d054c2b13)**: frontmatter task#/hash tokens scrubbed across all 123 pages; the catalogue that was the named remaining item (`defect-class-catalogue.md`, 101 KB, 71 hashes) is unpublished/moved in-repo; all 4 oversized `status_detail` blocks (meta/net/runtime/base) trimmed to ≤256 chars; refs repointed. | ~~P1~~ done | d054c2b13 | ✅ catalogue unpublished; 4 status_details ≤256 chars |
| D2 | ~~Stale statuses~~ **CLOSED 2026-07-16 (T0136, d054c2b13)**: `math` page reconciled; `base` unfrozen to the #55-wave truth; `runtime` unfrozen to wave-4; the named remaining item (INVENTORY had only 3 tracing rows vs the website's 9) is resolved — 6 tracing rows added + 3 refreshed = 9, verified present in `core-tests/INVENTORY.md` by this reconciliation. | ~~P1~~ done | d054c2b13 | ✅ 9/9 tracing rows confirmed in INVENTORY.md |
| D3 | ~~CLAUDE.md crate map drift~~ **CLOSED 2026-07-16** (85253727b): roster matches `crates/` (incl. verum_integration_tests); key-file cells fixed (verum_common → semantic_types.rs; verum_cbgr → real analysis files); import snippet corrected. | ~~P1~~ done | 85253727b | ✅ map matches `crates/` |
| D4 | ~~Dead file refs in docs/detailed~~ **CLOSED 2026-07-16** (85253727b + d135ad92d): per standing directive, public files no longer reference the internal docs tree at all — requirements stated in place or cited from public docs; repo-wide gate `make check-internal-refs`. | ~~P2~~ done | d135ad92d | ✅ zero internal-dir refs (gate) |
| D5 | ~~Grammar sync claim stale~~ **CLOSED 2026-07-16**: syntax doc claims v3.0 = ebnf v3.0; residual sibling v2.10 cites folded into T0138 (roadmap/status-docs rewrite). | ~~P2~~ done | agent-4 verification | ✅ version claims match |

## E. Infrastructure

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| E1 | ~~Shared build dir across sessions~~ **CLOSED 2026-07-16 (T0141, d054c2b13)** — session-private CARGO_TARGET_DIR + stage-only-your-paths are MANDATORY CLAUDE.md rules (85253727b); the named remaining item (CI-script compliance) is verified: `check_no_libc_link.sh` honors CARGO_TARGET_DIR, CI runners are already isolated, the `/tmp/target` smoke path's behavior is noted as intentional. | ~~P1~~ done | 85253727b, d054c2b13 | ✅ CLAUDE.md rules + CI scripts both isolated |
| E2 | `verum check` cannot validate stdlib modules standalone (relative mounts resolve only in bake context) — bake is the only validator, 6-minute cycle. (T0124) | P2 | channel.vr episode | `verum check --stdlib` mode or faster bake |
| E3 | Full-filter `verum test --filter <domain>/` intermittently SIGSEGVs the harness under parallel compile; per-suite filters are the workaround. (T0139) | P2 | tracing sessions | full-filter run stable |
| E4 | **Cross-session coordination** — ~~ad-hoc per-session task numbering, no shared work queue~~ **CLOSED 2026-07-16 (T0101, 85253727b)**: file-based task pool `.taskpool/` with global monotone IDs + atomic claims; protocol `docs/architecture/multi-session-taskpool.md`; commit-subject convention `(T0123)`. Legacy `#N` labels are frozen history (known collision: `#31` = default-args in memory vs MEM-BULK in INVENTORY). | ~~P1~~ done | 85253727b | ✅ pool live; new work referenced by pool IDs |
| E5 | **Cfg gates on undeclared cargo features** — 15 accidental `#[cfg]` sites (of 38 total) in shipping `src/` name a feature declared in NO Cargo.toml, so the gate is permanently FALSE and only the `not(...)` fallback is ever compiled. NOTE the 15 `verum_codegen`/`aot-llvm` sites are EXCLUDED from this row's scope: that gate is deliberate and self-documented in `mlir/aot/compiler.rs:170-205`, with a `tracing::warn!` naming the AotConfig fields the fallback cannot honour — a staged capability, not an accident. The accidental remainder is 4 `cfg_attr(feature = "serde", derive(Serialize, Deserialize))` in `verum_compiler` that therefore never derive. Found 2026-07-25 by sweeping every cfg site against declared features + optional deps. (T0633) | P2 | T0633 (full per-crate inventory) | every `cfg(feature=..)` in src/ names a declared feature, or the dead arm is removed |
| E6 | ~~**CI's only test gate did not COMPILE**~~ **CLOSED 2026-08-10 (T0533, 427343aa9)** — `cargo test --workspace --lib --bins` failed with `E0063: missing field origin_module` in three hand-built `FunctionDescriptor` fixtures; the v2.13 wire field was added without updating them. Red since 2026-08-08 and invisible to release builds, which never compile `#[cfg(test)]`. Attribution measured against the run two days earlier before concluding. | P1 | `crates/verum_vbc/src/{tests.rs,linker.rs}` | `cargo test -p verum_vbc --lib` compiles |
| E7 | ~~**Nightly built a package that does not exist**~~ **CLOSED 2026-08-10 (T0148, d7b2600c6)** — `cargo build -p vfuzz` had failed every night since the genesis commit: the package is `verum_vfuzz`, `vfuzz` is only its `[[bin]]` name. Two jobs died in under a minute each night while a third burned the full 60-minute ceiling alone. | P1 | `.github/workflows/nightly.yml` | `cargo pkgid -p verum_vfuzz` resolves; Nightly reaches its test steps |

## F. VBC-internals campaigns (added 2026-07-20)

Cross-cutting Tier-0/codegen campaigns discovered by the 2026-07 audits — same systemic silent-wrong/crash shape as Section A, previously tracked only in the pool.

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| F1 | **Extended-opcode envelope class-kill** — Ffi/Math/Simd/Char/System/Cbgr/Arith/Text/Log/Ml/Cubical handlers decided the next instruction's offset from their own per-arm read count instead of the encoded operand-length, so a one-register miscount desynced the whole instruction stream (SIGSEGV class). **CLOSED 2026-07-20**: ONE authority `handlers/envelope.rs::dispatch_enveloped` repositions pc from the envelope on every path, rustc-enforced (a family body cannot register in the dispatch table without routing through it); 3 duplicate per-handler copies deleted. Closed T0409/T0410/T0411/T0412/T0414/T0415/T0416/T0418/T0419/T0420/T0429/T0430/T0450. | ~~P2~~ done | 44343afc1 da30100b9 b0bb38dfc 25ed364cd f59611876 | ✅ 1767→1782 vbc tests, 0 regressions |
| F2 | **Cross-module call identity** — an unresolved cross-module `Call` silently identity-fell-back to an arbitrary same-id function (misroute). **CLOSED 2026-07-20 (T0144/T0114)**: ONE band-resolution authority at every call consumer + a NAMED unresolved-xmod diagnostic replacing the silent fallback; `VERUM_TRACE_CALLBIND` surfaces bare-name binding. | ~~P1~~ done | a068e0f89 a12de4287 | ✅ landed |
| F3 | **Scalar semantic honesty** — `compile_binary` derives the scalar-type fact three ways, derives width nowhere, and emits `if is_float {BinaryF} else {BinaryI}` with NO UNKNOWN state, so an opaque/generic operand gets the INTEGER opcode; ordered compare truncates floats (`launder(2.5) < launder(2.9)` → false); Int128 collapses to i64. | P1 | T0375/T0272/T0364/T0422/T0216/T0476 | ONE scalar-class authority both tiers; branch wt/scalar-semantics-v2 in verify |
| F4 | **Sub-opcode/opcode numbers have no uniqueness authority** — hand-picked per branch; two branches collided on 0x68 the same day; a missing AOT lowering arm is only a Warning (dst never written = silent miscompile). | P2 | T0461 | build-time uniqueness proof; unlowerable opcode fails loud |
| F5 | **Feature-gated / broken test targets hide rot** — `layout_invariant_verifier_tests` has not compiled since 2026-07-07; three targets (incl. two guardrails) never build under the default `cargo test -p verum_vbc` because `codegen` is not a default feature, so a red exit is explained away and a second defect hides behind it. | P2 | T0482/T0486 | clean tree exits 0; CI runs --all-features |

## G. Verification / proof debt (added 2026-07-20)

The SMT/refinement proof layer — Verum's headline feature — had no register presence.

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| G1 | **Refinement checker rejected every satisfiable refinement** — the value-free path asked validity-over-the-base-type (`∀x. P(x)`) instead of inhabitation (`∃x. P(x)`), so `Int{>0}`, `Int{>=0 && <=5}` etc. were all rejected (tell: counterexample OUTSIDE the range). Half-fixed by ROUTE (removed on `verum verify`, left live on `verum check` + LSP). Fix splits the two judgments into one authority; Unknown must not reject an inhabitation claim. | P2 | T0457 (+T0262 runtime asserts) | branch wt/verification-v2 in verify |
| G2 | **One bad axiom disables ALL reflection for a module** — `verify_cmd.rs` hands the reflection registry to Z3 as one string; a single entry naming an undeclared symbol makes Z3 drop the whole block, so the proof status of a theorem depends on unrelated file content (demonstrated: appending one unrelated theorem flipped tactics.vr from 51/9 to 60/1). Root of ~76 of 113 failing core/ proofs. | P1 | T0489 | stable 60/0 + perturbation invariant; branch wt/verification-v2 |
| G3 | **T0354 proof burndown (113 failing core/ proofs)** — decomposed: the `==`-binds-tighter-than-`&&` mis-parse of 19 boolean-algebra laws is FIXED (88629f3d5, count-neutral prerequisite); remainder tracked as T0489 (76, reflection), T0490 (14 higher-order/monad-law), T0491 (18 method/protocol-call), T0480 (result-binder name capture), T0481 (literal-true unproved), T0487 (method-call P-or-not-P). | P2 | T0354 (decomposed) | children burned down + census re-measured post-T0489 |
| G4 | **209 verification tests have NEVER compiled** — 7 test files carry a file-level `#![cfg(feature = "X")]` where X is declared in no Cargo.toml, so the gate never fires and the file is silently dropped from the build: 4820 lines / 209 `#[test]` fns that have never once run. **Six of the seven are `verum_smt`** — dependent_types 79, comprehensive 31 (old Z3 API), advanced_features 26, dependent_types_v2 23, model_extraction 13 (old Z3 API), tensor_refinement 11 — plus verum_integration_tests/end_to_end 26. So effective SMT/refinement coverage is materially below what the tree advertises, which is the same false-green this section was created to surface. 2026-07-25: gates rewritten to `#![cfg(any())]` with in-file reasons so the exclusion stops masquerading as an opt-in flag (6dcc99640) — that is honesty, NOT a fix. (T0632) | P1 | T0632; `crates/verum_smt/tests/*` | per file: revived (compiles, runs, passes, gate deleted) or deleted with its intent recorded |

---

Cross-references: `docs/architecture/multi-session-taskpool.md` (the work
queue this register feeds — `scripts/taskpool/tp list open`),
`docs/architecture/tier-coherence-pillars.md` (P1–P5
pillar plan retiring §40/§46/#27), `docs/architecture/intrinsic-dispatch-contract.md`
(Rules 4–5 open work), `core-tests/INVENTORY.md` (per-module truth),
memory session file `session_2026-07-09_tracing_analysis_and_docs.md`
(18 toolchain-constraint classes with repros).
