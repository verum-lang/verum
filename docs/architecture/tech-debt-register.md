# Verum Technical-Debt Register

Status: LIVING DOCUMENT — single consolidated inventory of open debt
across every layer, with priorities and acceptance criteria. Produced
from a five-lane repository audit (conformance pins, compiler gap
surface, Rust hygiene, documentation drift, stdlib structure) on
2026-07-11; update rows in place as items close.

RECONCILED 2026-07-16 (T0102, five-agent audit): verdicts/anchors refreshed
row-by-row; every actionable row now carries its task-pool ID (`T0xxx`, see
`docs/architecture/multi-session-taskpool.md`) — claim the task before
working a row. New rows A22–A28 added from the audit.

Priorities: **P0** = wrong results or masked failures reach users;
**P1** = correctness debt with a bounded blast radius or a workaround;
**P2** = hygiene/polish that slows work but does not corrupt results.

---

## A. Compiler / runtime (Rust crates)

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| A1 | **CallM const-zero degrades** — unresolved method calls lower to `dst = 0` silently (measured 4210 sites in a hello-world binary). Root cause named in-code: VBC-GENERIC-INSTANTIATION (no generic-monomorphization-context propagation). Strict gate exists but is opt-in. **2026-07-16: STILL OPEN, anchors refreshed (T0103).** | P0 | `llvm/instruction.rs:9759` (UNRESOLVED_FN_ID→const_zero), `:19098` (CallM unresolved), gate `llvm/error.rs:668` | `VERUM_STRICT_MONO=1` clean on the core-tests corpus; then flip strict to default |
| A2 | ~~Tensor/GPU unknown sub-ops silently write 0~~ **CLOSED 2026-07-11**: both fallthroughs now route to `emit_unimplemented_sub_op("TensorExtended"/"GpuExtended", sub_op)` before the keep-alive const-zero — gaps surface in diagnostics instead of miscompiling silently. | ~~P0~~ done | fix commit on main (`lower_tensor_extended`/`lower_gpu_extended`) | ✅ fallthroughs report |
| A3 | **Lenient-SKIP BugClass warnings** (#166). BURN-DOWN 2026-07-12: **176 → 132 → 82 → 64 → 53 → 39 → 31 → 21** (seven consecutive zero-new bakes) (bakeX/Y/Z all zero-new). bakeX −50: arity ×5, f-string-capture class (~30 fns incl. parse_*/get_*/select/QuicServer.bind), wave-5 ×17 files. bakeY −18: wave-6 onion ×9 files (match-arm-RHS bare variants — arm-filter blind spot; single_int0_i64 = typo'd nonexistent fn → single_int(0)), wave-6b receiver-drift impls (GPUDevice ComputeDevice statics read self with no self param; is_covering dropped protocol's &self; commutativity_coherence self.act in receiver-less family), wave-6c @cfg(macos) block-scoped fd + peer_cred super.darwin.* mis-root, progressive core.-suffix resolution (resolve_core_rooted_suffix: longest registered proper suffix ≥2 segments, wired into qualified-path/field-access/method-call — closes 'undefined variable: core' in ALL expression shapes). bakeZ −11: FreeVarAnalyzer EXHAUSTIVE ExprKind coverage (39 of 73 kinds fell into the capture-dropping catch-all — select/comprehensions/spawn/try*/…; catch-all DELETED so a new ExprKind fails compile), pattern-position canonicalization (module-path-to-variant), phantom names materialized (ExtIoErr ctors) or corrected (WfText→FmtText, single_int0_i64→single_int(0)). CLOSED SINCE: stage-1 was BLIND to builtin-TypeKind impl targets ('implement Text' parses as TypeKind::Text, not Path — 694 stubs, zero Text.*; extractor now maps builtin kinds) → all 16 Text.* consumers green; phantom-API drift class realigned (Channel.unbounded→unbounded_channel, Duration.seconds→from_secs, from_bytes_lossy→from_utf8_lossy) + two GENUINE owner APIs added (Text.from_cstr on load_byte intrinsic; Instant.from_nanos) + BinaryHeap added to canonical set. CLOSED SINCE: **stage-5 mount-miss named stubs** (explicit braced-mount item + producer-not-yet-compiled + simple name NOT globally unique → call-site-synthesized stub with the call's arity under the mount's FULL qualified path; ArchiveBodyRemap name-chase resolves unambiguously; new STAGE5 band in verum_vbc::stub_ranges) — open_admin family, flags_default, access_name, kqueue/kevent/open, hedberg all green; jwt.decode_header_only phantom materialized at owner (OIDC key-selection primitive). REMAINDER 21→(bakeAJ pending): PROOF-IRRELEVANCE-ERASURE-1 landed (axioms callable as erased witnesses: LoadTrue for Bool props, LoadNil for dependent proofs — hott x4 + hedberg + ua + infinity_topos partially; truth stays with verification phase) ≈ −7; uppercase-locals RECLASSIFIED into (c) via bakeAI [undef-var] evidence (params AND lets lose bindings inside meta-generic impls — var_reg_err, is_type_name=false: ssm B/B_bar, nn H, rag neighbors — peer v11 impl-generics-carry domain, N/DIMS/k same root). B1 layer-2 ROOT FIXED: ALIAS-INTRA-MODULE-ORDER-1 — declared_alias_target_name's extra type_name_to_id check dropped `type IoError is StreamError;` (mod.vr evaluated before sibling protocols.vr registered the target; bakeAI [static-alias] alias_hit=false witness); local-visibility scope gate is now the sole evidence when the oracle is present. Classes:  (c) meta const-generics N/DIMS/k ×9 (peer impl-generics-carry pillar); NEW (f) uppercase locals B/B_bar/A_scaled ×3 mis-classified in expr position (ssm.vr params — needs trace); NEW (g) proof-surface calls (axiom-as-term hott.vr ×3, protocol-field functor composition infinity_topos ×2) — verification-layer design item. Flip-to-hard-error gated on (a). || A5 | **Shared-identity / payload-extraction fork** — extracting `Shared<T>` from an enum payload copies the cell; cross-handle shared mutable state is impossible from .vr. Blocks 9 pinned tracing-pipeline tests and any stateful in-memory pattern. **2026-07-16: STILL OPEN (T0107)** — 9 pins live at `tracing/pipeline/unit_test.vr:96-442`; the SHARED-STRONGCOUNT-1 deref arm (`cbgr.rs:239`) does NOT cover extraction. | P1 | pinned `[[tracing-delivery-vbc-class]]`, `[[shared-identity-vbc-class]]` | tracing pipeline pins un-ignore and pass |
| A6 | **Protocol-object (dyn) dispatch inoperative in the interpreter** — all receiver forms mis-bind. Stdlib now routes through closed-world handle enums; `…Custom(Shared<dyn …>)` seams are dormant. **2026-07-16: STILL OPEN (T0106)** — DYN-MISS-LOUD-1 (`method_dispatch.rs:2783`) made interp misses loud, not correct; AOT wrong-impl `#76` still @skips the vcs spec. Bare-name shadow leg = T0146. | P1 | probes in memory session file; vcs `#76` (AOT wrong-impl dispatch) | a `Shared<dyn P>` method call dispatches correctly on both tiers |
| A7 | **Bake nondeterminism (string-dice)** — same sources produce different stub-name resolution across bakes. **2026-07-16: PARTIALLY CLOSED** — deterministic total order `canonical_name_better` landed (`codegen/context.rs:33-45`, 4a667d087); byte-identity acceptance still unproven (T0120). Re-export leg split out as A20. | P1 | `VERUM_TRACE_STRDICE`; `codegen/context.rs:33` | two consecutive bakes byte-identical; AOT suite counts stable |
| A8 | **AOT reference-return classes** — `&Text`/`Maybe<&T>` returns crash or mis-read under AOT. **2026-07-16: PARTIALLY CLOSED** — facet 1 live-green (`QueryRow.get_index → Maybe<&Text>`, 072fafab4; `Text.as_bytes` 1aad1409a); residual `[[btree_pattern_match_ref_generic_class]]` family in 10+ suites (T0109; record-OOB leg = T0147). | P1 | grep the family marker | family pins green under `--aot` |
| A9 | **FFI placeholder arms** — `GetLibrary` returns null "for now", `IsSymbolResolved` hardcodes true, 9 MachVm*/MachSem* arms emit const-zero unconditionally, CreateCallback passes through. **2026-07-16: STILL OPEN, anchors refreshed (T0110); Tier-0 twin `__fd_read_*`→0 = A28/T0111.** | P1 | `instruction.rs:28665`, `:28685`, `:29195`, `:29992` | real implementations or explicit unimplemented markers |
| A10 | **SIMD scalar-width-1 degrades** — 30+ SIMD arms lower as documented scalar fallbacks; MaskedStore/Scatter are silent no-ops (writes dropped). **2026-07-16: STILL OPEN, anchor refreshed (T0112).** | P1 | `instruction.rs:25228` family, no-op stores `:25537` | either real vector lowering or loud unimplemented |
| A11 | **Tier-0 futex** — ~~FFI gap~~ ROUTED 2026-07-12: platform futex_wait/wake wrappers now call the canonical intrinsics (InlineSequence FutexWait/Wake → Tier-0 futex_park parking table; AOT verum_futex_* shims). Raw __ulock/SYS_FUTEX/WaitOnAddress FFI left the Tier-0 hot path. **2026-07-16: suites still pinned (24 pins: barrier §3.1 ×15, condvar §3.6 ×9) — re-run + flip = T0113.** | P1 | `sync/barrier/*`, `sync/condvar/*` pins | barrier/condvar suites green under `--interp` |
| A12 | **CSPRNG intrinsic gap for `fill_secure` consumers.** **2026-07-16: MOSTLY CLOSED** — `random_u64` registered (`intrinsics/registry.rs:5840` + handler), reservoir ×4 un-ignored (d9ebc7f9d), backoff un-ignored earlier. Residual bloom pins re-diagnosed to a DIFFERENT root: slice-passing/hmac_sha256 (#17 family, T0114); the architectural upstream (collections↔security topo-cycle → cross-module Call StringId encoding, INVENTORY.md:210) = T0144. | P1 | `collections/bloom/regression_test.vr:60-95` | bloom pins un-ignore |
| A13 | ~~**verum_types unreachable-pattern arms (9)** — real logic smells in inference.~~ RESOLVED 2026-07-15 (5cfb3c0e3): modules.rs duplicate `Type::Var` arm removed; expr.rs Numeric-guarded `min\|max` duplicate removed; types.rs broad `(Int\|Named, Pointer\|Volatile)` cast arm split so the sized-integer-alias restriction on the `Named` side is restored (was silently accepting `ptr as AnyStruct`). 9→0 unreachable warnings; verum_types tests 1035/0; stdlib bake green. | P2 | `infer/expr.rs`, `infer/modules.rs`, `infer/types.rs` | ✅ warnings gone via fix (not allow) |
| A14 | **Codegen panic surface** — **2026-07-16: GREW to 621** unwrap/expect in `llvm/` (instruction.rs 393, runtime.rs 163; baseline was 501/319/156): malformed VBC crashes the compiler instead of a diagnostic. (T0131) | P2 | audit-warnings §3 | builder unwraps in top-10 hottest paths converted to diagnostics; count trend reversed |
| A15 | **Blanket `#![allow]` hides dead code** — verum_codegen/verum_verification suppress everything; ~29 confirmed-dead pub fns in `llvm/` (SIMD/bitfield/cbgr/wasm clusters). **2026-07-16: unchanged (T0132)** — `verum_codegen/src/lib.rs:1-5`, `verum_verification/src/lib.rs:1-3,81-84`. | P2 | audit-warnings §1–2 | blanket allows removed; dead fns deleted; warning budget real |
| A16 | **Cross-compile host-default risk** — `for_host()` is the default no-libc config path; verify `--target` overrides on cross builds. **2026-07-16: anchor refreshed (T0123)** — `link.rs:573`; override path exists (`linking.rs:301` → `for_platform` `link.rs:560`) but unproven. | P2 | `verum_codegen/src/link.rs:573`; callers `verum_compiler/phases/linking.rs:244,325` | cross-build test proves target config wins |
| A17 | **`static mut` cells >8 bytes fall back to TLS; non-zero initializers need ctors** (open work inside intrinsic-contract Rule 4); CBGR bound-check `1<<24` is a stated workaround pending NaN-box tag (Task F4). **2026-07-16: unchanged (T0133)** — TLS fallback `interpreter/state.rs:658-670`; `CBGR_REF_ABS_INDEX_MAX` `state.rs:2891`. | P2 | intrinsic-dispatch-contract.md Rules 4–5 | wide cells + initializers native; TAG_CBGR_REF lands |
| A18 | **Mono persistent-cache read side disabled — needs a portable materialiser** (2026-07-13, bfc328704): `ResolvedSpecialization::Cached` had NO consumer (merger materialises only `StdlibPrecompiled`), so every ~/.verum/cache/mono hit SWALLOWED its instantiation (pending=0, mono no-op). Read consult disabled in `mono/resolver.rs resolve_one` (write side + `validate_cache` retained). Real fix: a cached-function format that carries its string/type/function id maps so the merger can decode + remap into the target module. **2026-07-16: unchanged (T0134)** — `mono/resolver.rs:294-295`, `Cached` consumer-less at `:341-343`. | P2 | `mono/resolver.rs` Step-2 comment; memory: session_2026-07-13 | cache read re-enabled behind a materialiser; cross-module id remap proven by a differential test |
| A19 | **MEM-PTR-DEREF-TIER0-1 — Tier-0 typed-pointer deref/arith lacks the two-worlds provenance model** (2026-07-15): `ptr_read` is an identity stub / `ptr_write` a silent no-op on the interpreter (AOT correct — cross-tier incident); ptr arithmetic is Value-slot-strided (×8) for every pointee. One `*const T` fronts TWO physical layouts (NaN-boxed Value arrays vs packed bridge/Text/typed-array bytes) — static types cannot discriminate. Design recorded in session task #16: bridge-allocation interval index + PtrReadT/PtrWriteT/PtrAddT {width} sub-ops with runtime provenance branch + AOT twins. **2026-07-16: STILL OPEN (T0108)** — pins live at `intrinsics/memory/property_test.vr:187,:334`; `__ptr_read_i64`→0 `calls.rs:2543`; `DEFAULT_STRIDE=8` `expressions.rs:29193`. | P1 | `handlers/cbgr.rs:165 handle_deref`; `expressions.rs ptr_intrinsic_byte_stride` | memory property §8 pins (`law_ptr_write_read_round_trip_over_extremes`, `law_ptr_add_byte_stride_is_one`) un-ignore green BOTH tiers |
| A20 | **UMBRELLA-REEXPORT-RESOLVE-1 — wildcard re-export resolution через umbrella brace-mounts недетерминирована per run** (2026-07-15): two runs of one suite on one binary produced different E100 unbound sets (`wrapping_add`/`popcnt`/`nop` vs `saturating_add`/`rotl`/`is_nan`/`likely`); explicit re-export lists stable; `memory_fence` MIS-BINDS to a 1-arg fence. The function-name leg of the A7/METADATA-DETERMINISM family (types leg was sorted; re-export walk was not). **2026-07-16: PARTIALLY CLOSED (T0115)** — one-hop qualified-key probe landed (a74924270); acceptance blocks still commented (`intrinsics/mod/unit_test.vr:104-114`; the runtime/mod @ignores at :55-104 are a different class). | P1 | acceptance blocks committed commented in `core-tests/intrinsics/mod/unit_test.vr` + `runtime/mod/unit_test.vr` | the two acceptance blocks promoted to live tests, green across 3 consecutive runs |
| A21 | **LITERAL-SIZED-ALIAS-COERCE-1 — bare Int literals no longer coerce to USize/ISize/Byte params of baked free fns** (2026-07-15, surfaced by the honest v20 metadata — the params previously parsed opaque and unified with anything): `ptr_add(bp, 1)` E400s, `1 as USize` passes; the arg diagnostic renders params[0]'s type while checking arg[i] (off-by-one). Coercion machinery already exists (`unify.rs is_int_coercible_named` / NUMERIC_ALIAS_MATRIX) — the metadata-fn call-arg CHECK path bypasses it. ~~**CLOSED 2026-07-15/16** (34129a636, commit-labelled "A23"): 55 signature-satisfaction casts dropped, 0 residual `as USize/ISize` across the memory suite; over-arity diag now E102 spanned+named; memory 46/46.~~ | ~~P1~~ done | 34129a636 | ✅ bare-literal calls typecheck; casts removed |
| A22 | **CVT-PASSTHRU-1 — `CvtToI`/`CvtToF` lower as bit passthrough** at Tier-1 ("for now just pass through"): dynamic int↔float conversions return the SOURCE BITS unconverted — silent wrong numerics, cross-tier divergence (interp converts). Likely explains part of the intrinsics conversion/float AOT partials (T0153). | P0 | `llvm/instruction.rs:2436` | real fptosi/sitofp (or tagged dispatch); differential conversion test both tiers (T0104) |
| A24 | **PROOF-TACTIC-ACCEPT-1 — proof checker accepts ANY tactic** (`Ok(())` for every `ProofStepKind::Tactic`): invalid proofs pass silently — soundness hole in the verification story. | P0 | `verum_types/src/proof_checker.rs:387` (+`:442,:604,:613,:626,:823`) | tactic steps validated or explicitly diagnosed as unverified; negative test (T0105) |
| A25 | **Structural protocol conformance returns true unconditionally** for `Named` types ("for now, return true") — method-absence errors masked. | P1 | `verum_types/src/protocol.rs:7194` | conformance verifies method presence/signatures; negative test (T0117) |
| A26 | **Empty/stub fn bodies accept any return type** — return-type mismatch unchecked for stub bodies. | P1 | `verum_types/src/infer/modules.rs:10079` | stub bodies type-check against declared return (T0118) |
| A27 | **Leaked `Placeholder` unifies with anything** — intended only for forward-declared recursive types. | P2 | `verum_types/src/unify.rs:4430` | placeholder unification scoped; leak-detection test (T0119) |
| A28 | **Tier-0 `__fd_read_all_raw`/`__fd_read_chunk_raw` return 0 "no data"** — `Child.read_stdout` silently empty under the interpreter (Tier-0 twin of A9's fake-success class). | P1 | `handlers/calls.rs:2535` | real fd reads; subprocess stdout test green under `--interp` (T0111) |

## B. Conformance debt (core-tests / vcs)

173 `@ignore` pins across 54 files; ~90 are genuine compiler-defect
pins collapsing into the classes below; ~40–77 are the *absence of a
network test harness* (one infra item, B4); 676 audit-gap entries are
mostly API-surface polish (B5).

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| B1 | **type_id=0 field-access OOB — CLOSED 2026-07-12.** TUPLE-POSITIONAL-FIELD-1 (layer-1) + ALIAS-INTRA-MODULE-ORDER-1/Phase-2.9/mount-provenance (layer-2, alias_hit=true bakeAR witness) landed & fence-pinned. probe_udp now advances through construction + alias-static + syscall. RESIDUAL is a DIFFERENT class → see B1b. | done | fence markers | ✅ construction + dispatch correct |
| B1b | **FFI-BYTE-BUFFER-STRIDE-1 — bind/send CLOSED 2026-07-12** (bakeAW: `bind_a_ok` + `sent=4` through the embedded archive). serialize_socket_addr keeps returning List<Byte> (correct values + explicit length, no NUL-truncation — the Text-wrap attempt failed on strlen); each of the 8 socket syscall sites copies into an IN-SCOPE packed [Byte;28] and passes &sa[0] + explicit count. Pure .vr. RESIDUAL on recv: distinct bug → B1c (@ffi_name ignored). Then real loopback round-trip test replaces the placeholder @ignore stubs. | done (bind/send) | probe_udp bind_a_ok/sent=4 | ✅ bind + send |
| B1c | **@ffi_name/@link_name ignored — CLOSED 2026-07-12** (fence-markered). extract_ffi_symbol_name + ffi_symbol_link_names side-map + build_module prefers it for the dlsym key; fixes mmap/recvfrom/rename/symlink/vm_allocate. bakeAX: recvfrom now RESOLVES (no more "symbol 'recvfrom_ffi' not found"; probe_udp reaches recv). | done | codegen/mod.rs | ✅ symbol resolves |
| B1d | **UDP recv path — .vr FIX LANDED 11a6aafb6 (NO marshaller/ctype).** The ctype-driven marshaller was the WRONG frame: the packed byte-buffer representation ALREADY exists (`[Byte;N]` annotation → NewByteArray → TypeId::U8 contiguous; `container_to_slice_fat_ref` U8→reserved=1 → `.as_mut_ptr()`/Unslice yields the packed data ptr). Four .vr fixes: (1) safe_recvfrom used `transmute(buf)` on the slice VALUE (fat-ref struct bits) → `.as_mut_ptr()` (matches getsockname/getsockopt); (2) `addr_buf=[0_u8;128]` (NewList reserved=0 strided) → `[Byte;128]=[0;128]` packed, passed as subslice `&mut addr_buf[..]`; (3) deserialize_socket_addr read family@buf[0] but BSD sockaddr_in is {sin_len@0,sin_family@1} → macOS reads buf[1] (udp+tcp); (4) local_addr → live getsockname. Layer 5 (83be4c7aa): `&addr_buf[..addr_len as Int]` mis-parsed as `(&addr_buf[..addr_len]) as Int` (cast binds to the whole range) → parenthesize `[..(addr_len as Int)]` at all 11 deserialize sites (a for-loop `0..n as Int` parses correctly — slice-context quirk). EMPIRICAL RULE: packed `[Byte;N]`→`&[Byte]` param is packed ONLY via SUBSLICE `&buf[..]`; bare `&buf`/`&mut buf` does NOT coerce → **SLICE-COERCE-ARR-1 (task #24)** the footgun-killer. **VERIFIED (interp) on a clean private build: probe_udp → ROUNDTRIP_OK** (local_addr=127.0.0.1:64217 real ephemeral port, sent=4, recv=4, from=V4(127.0.0.1:61880), bytes="ping"). Contract pinned `docs/architecture/ffi-byte-buffer-contract.md` (89d95451d). AOT mirror BROKEN (getsockname ABI i64-vs-i32; sent/recv/port=0) → **task #26**; socklen write-back → #25. | **interp DONE ✓** (11a6aafb6+83be4c7aa+aab469ef0); AOT #26 | ffi-byte-buffer-contract.md; tasks #24/#25/#26 | ✅ interp ROUNDTRIP_OK || B6 | **INVENTORY taxonomy drift** — **2026-07-16: PARTIALLY CLOSED (T0137)**: INVENTORY legend now defines `stable` (all 5 statuses defined); website `status-convention.md` still omits `stable` (legends not unified); INVENTORY claims `undocumented` unused but `context/layer` uses it. | P2 | INVENTORY.md legend; website `status-convention.md` | one shared taxonomy, both legends updated |
| B7 | **vcs real blockers** — #143 Waker.drop NPE (stdlib-cache codegen bug, @skips L1/L2 specs incl. congruence_closure/aes_block_kat/page_roundtrip — T0125), #76 AOT protocol-method wrong-impl dispatch (= A6 leg, T0106). **2026-07-16: both OPEN.** | P1 | `vcs/specs/L0-critical/_codegen_regressions/aot_to_socket_addrs_dispatch.vr:6` | both specs pass |
| B8 | **Pin census 2026-07-16**: 670 `@ignore` in core-tests `*.vr` + 1 vcs flaky (T0164). Dominant cross-suite classes with pool tasks: bare-name dispatch collisions (io 48, T0146), `[[btree_pattern_match_ref_generic_class]]` + BTreeMap SetF corruption (collections 90, T0147), base #55 wave 16 families (T0148), net codegen crashers (T0149), meta static-dispatch OOB (57, T0150), term Range-iter (18, T0151), AOT tier-partials (net/addr 35F T0152; intrinsics float/conv/atomic T0153; text #41-tail 39F T0154). | P1 | agent-3 census, session 187337bc | per-class tasks close; census re-run trends to zero |

## C. Stdlib (.vr) structure

Scope measured: 2546 .vr files / 581K LOC; 5820 `public type` decls;
8696 mounts; 1374 `@intrinsic` occurrences. Full enumerations +
reproducible scripts: audit scratchpad (cat1_dups.txt, cat2_drift.txt,
cat4_unresolved.txt).

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| C1 | **Duplicate public type names** — 155 names declared in ≥2 files (326 decls); 64 kind-divergent genuine collisions, of which **27 are protocol-vs-record hard collisions**. **RESOLVED 2026-07-11 (26 fixed + 1 assessed-by-design)** (batches 1-4: sqlite-internal Capability family unified, Capability family unified onto database/common; CommitDecision/AuthDecision/UpdateOp/ProgressDecision deduped to their authorities; TraceEvent→TraceEventKind, BusyDecision→BusyHookDecision (ms-vs-ns unit honesty), TraceCtx→TraceHookCtx). verify-kernel cluster ASSESSED, no action: kernel_v0 (bootstrap .vr kernel formalization) and kernel_soundness (propositional carrier for theorems about the Rust proof kernel) are two INDEPENDENT formalizations whose shared vocabulary (Judgment/Context/Derivation) is domain-canonical in each; verify/mod.vr already isolates both from flat re-export (qualified-path access only, documented in-file), so the bare-name first-wins surface is designed out. Renaming formal-methods vocabulary would damage domain honesty for no dispatch benefit. (InclusionProof resolved: merkle keeps bare; Rekor/Signed wire names; wire-shape merge tracked as follow-up.) **2026-07-16 RE-SCAN (agent-5)**: 130 duplicate names remain (was 155); **53 kind-divergent** (22 protocol-vs-data dispatch hazards — full table in the agent report/T0156; 30 data-vs-data; 1 intentional `@cfg` alias SockaddrIn); 62 same-kind twins + 15 domain-canonical formal-methods names (safely qualified, zero wildcard re-exports — no action). Top-10 shape-verified twins → T0155; 22 hazards → T0156. | P1 | 2026-07-16 scan (session 187337bc) | 22 protocol-vs-data hazards renamed/qualified (T0156); top-10 twins unified (T0155) |
| C2 | ~~Mount drift~~ — **CLOSED 2026-07-11** (commit 7eca17515): 44 broken mounts → 0, detector re-run clean, bake-verified. | ✅ | cat2_drift.txt | done |
| C3 | **Unregistered `@intrinsic` calls** — 564 distinct names / 578 occurrences; miss = silent LoadNil (`expressions.rs:26463`) or Unit fallthrough. PROGRESS 2026-07-11 (commit 968f09f80): the LIVE silent-nil kernel rerouted to registered implementations — llvm.*.with.overflow ×6 (checked_add_u/checked_sub_u destructured nil tuples!), sincos ×3 (composed sin+cos), verum.float bit-casts ×5, verum.rng.fill_secure ×2 (→ canonical rng helper), mysql float_to_bits ×2. REMAINING: (a-residue) GPU set / SIMD compares / checked_rem/shl/shr / overflowing_neg/shl/shr / widening_mul / carrying_add / transmute / ptr_read_* / cpu_feature — need registry entries + Tier-0 handlers (Rust); (c) ~250 `verum.<host>.*` deliberate host-subsystem stubs (tls/quic/h3/k8s/xds/compress/cloud) — separate not-yet-implemented track. **2026-07-16: miss path now WARNs at bake (`expressions.rs:28807`) but still LoadNil at runtime (`:28813`); (a)-residue registry count still 0 → T0116.** | P1(a)/P2(c) | `codegen/expressions.rs:28807` | (a) registered with handlers; runtime miss loud |
| C4 | ~~Dead/drifted surface~~ — **CLOSED 2026-07-11** (f15dae9e9): AllocHandle/JoinHandleOpaque intrinsics-side alias decls deleted (Raw* canonical); `contracts_old` re-diagnosed as FALSE POSITIVE — it is the live 'contracts with old() pre-state refs' surface, retained. | ✅ | — | done |
| C5 | **Toolchain-constraint compliance** — 18 documented .vr-breaking classes; stdlib predating the map may violate them silently. **2026-07-16 SWEEP (agent-5)**: `::` in live code = 0; relative qualified statics in live code = 0 (12 hits all comments); wide literals = 16, all intentional wide-type consts in `base/primitives.vr` (verify lexer path); compound index-assignments = 1115 sites (mostly packed arrays — verify the constraint still exists before any sweep, T0157). | P1→P2 | 2026-07-16 scan | T0157: constraint re-verified on HEAD; close or lint+fix |
| C6 | **Byte-conversion fn-form intrinsics return non-containers** — `@intrinsic("to_le_bytes"/"to_ne_bytes"/"to_be_bytes")` (fn-forms in `core/intrinsics/conversion.vr`, declared `-> [Byte; N]`) lower to a raw i64 reinterpret (`InlineSequenceWithWidth(ToLeBytes, 8)` → `emit_to_le_bytes` = plain `Mov`, `intrinsics/codegen.rs:2530`; MLIR leg = `arith.bitcast`, `intrinsics/lowering.rs:1385`). Any consumer treating the result as a container (`bytes.len()`, `bytes[i]`, slice-typed arg positions) gets garbage on BOTH tiers. Discovered 2026-07-13 as the root of #44-B (`Hasher.write_int` default fed a raw Int into `write`; the DefaultHasher intercept silently hashed nothing → equal hashes). Hasher path detached from it (per-byte `write_byte` chain); METHOD-forms (`n.to_le_bytes()`) are correct (interp allocs a real list, `method_dispatch.rs:4835`). Fix = container-honest lowering: dedicated instruction (RandomU64 pattern: TensorExt sub-op + Tier-0 handler + LLVM lowering) allocating a real `[Byte; N]`, or retire the fn-forms onto the method-forms. **2026-07-16: unchanged (T0130)** — `intrinsics/codegen.rs:2547/:2555`, MLIR `lowering.rs:1422`; method-forms correct (`method_dispatch.rs:4891`). | P2 | this row | fn-forms produce real byte containers on both tiers (or are retired); grep shows no consumer of the Mov-reinterpret shape |

## D. Documentation

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| D1 | **Website policy violations** — **2026-07-16: PARTIALLY CLOSED (T0135)**: frontmatter task#/hash tokens scrubbed across all 123 pages; REMAINING: `defect-class-catalogue.md` (101 KB, 71 hashes) still published + 4 oversized `status_detail` blocks (meta/net/runtime/base, 1.2–2.7 KB vs the 256-char contract). | P1 | website `docs/stdlib/` | catalogue unpublished/rewritten; 4 status_details ≤256 chars |
| D2 | **Stale statuses** — **2026-07-16: PARTIALLY CLOSED (T0136)**: `math` page reconciled; REMAINING: `base` detail frozen at 2026-06-02; INVENTORY has only 3 tracing rows vs the website's claimed 9 green suites. | P1 | audit-docs part 2 | spot-check table all "consistent" |
| D3 | ~~CLAUDE.md crate map drift~~ **CLOSED 2026-07-16** (85253727b): roster matches `crates/` (incl. verum_integration_tests); key-file cells fixed (verum_common → semantic_types.rs; verum_cbgr → real analysis files); import snippet corrected. | ~~P1~~ done | 85253727b | ✅ map matches `crates/` |
| D4 | ~~Dead file refs in docs/detailed~~ **CLOSED 2026-07-16** (85253727b + d135ad92d): per standing directive, public files no longer reference the internal docs tree at all — requirements stated in place or cited from public docs; repo-wide gate `make check-internal-refs`. | ~~P2~~ done | d135ad92d | ✅ zero internal-dir refs (gate) |
| D5 | ~~Grammar sync claim stale~~ **CLOSED 2026-07-16**: syntax doc claims v3.0 = ebnf v3.0; residual sibling v2.10 cites folded into T0138 (roadmap/status-docs rewrite). | ~~P2~~ done | agent-4 verification | ✅ version claims match |

## E. Infrastructure

| # | Item | Pri | Anchor | Acceptance |
|---|------|-----|--------|------------|
| E1 | Shared build dir across sessions — peer relinks `target/release/verum`; peer WIP breaks `cargo build` mid-flight. **2026-07-16: PARTIALLY CLOSED** — session-private CARGO_TARGET_DIR + stage-only-your-paths are now MANDATORY CLAUDE.md rules (85253727b); CI-script compliance check = T0141. | P1→P2 | CLAUDE.md coordination section | CI/iteration scripts use isolated target dirs |
| E2 | `verum check` cannot validate stdlib modules standalone (relative mounts resolve only in bake context) — bake is the only validator, 6-minute cycle. (T0124) | P2 | channel.vr episode | `verum check --stdlib` mode or faster bake |
| E3 | Full-filter `verum test --filter <domain>/` intermittently SIGSEGVs the harness under parallel compile; per-suite filters are the workaround. (T0139) | P2 | tracing sessions | full-filter run stable |
| E4 | **Cross-session coordination** — ~~ad-hoc per-session task numbering, no shared work queue~~ **CLOSED 2026-07-16 (T0101, 85253727b)**: file-based task pool `.taskpool/` with global monotone IDs + atomic claims; protocol `docs/architecture/multi-session-taskpool.md`; commit-subject convention `(T0123)`. Legacy `#N` labels are frozen history (known collision: `#31` = default-args in memory vs MEM-BULK in INVENTORY). | ~~P1~~ done | 85253727b | ✅ pool live; new work referenced by pool IDs |

---

Cross-references: `docs/architecture/multi-session-taskpool.md` (the work
queue this register feeds — `scripts/taskpool/tp list open`),
`docs/architecture/tier-coherence-pillars.md` (P1–P5
pillar plan retiring §40/§46/#27), `docs/architecture/intrinsic-dispatch-contract.md`
(Rules 4–5 open work), `core-tests/INVENTORY.md` (per-module truth),
memory session file `session_2026-07-09_tracing_analysis_and_docs.md`
(18 toolchain-constraint classes with repros).
