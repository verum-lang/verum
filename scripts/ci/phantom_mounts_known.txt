# Mounts naming a symbol the module does not export.
# Regenerate: scripts/ci/check_phantom_mounts.py <verum> --write-baseline
# A line removed is a mount repaired.  A line added is a decision to
# ship one more import that resolves to an unrelated same-named symbol.
#
# NOT every line here is a misspelling.  Names declared inside an
# `extern` block are absent from the archive metadata (T0781), so a
# mount naming one is reported even though the declaration exists in
# the module's source.  On this baseline that is the libsystem group
# — close, open, kqueue, kevent — and repairing those by renaming
# would break working FFI.  Read the module source before treating a
# line here as a typo.
#
# This paragraph lives in the SCRIPT, not only in the file it writes:
# the first regeneration after it was added by hand silently dropped
# it, which would have left the next reader with seven lines that
# look like misspellings and are not.
core.database.common.types	WfBinary
core.database.common.types	WfText
core.database.sqlite.native.drop_modules.keep_list	contains
core.database.sqlite.native.l3_btree.cursor	generate_series
core.database.sqlite.native.l5_sql	VdbeProgram
core.database.sqlite.native.l7_api	Transaction
core.encoding.der	DerWriter
core.encoding.der	Time
core.intrinsics	RecoveryError
core.math.s_definable.class_s_s	msfs_refined_ss_depth_nonneg
core.math.s_definable.class_s_s	msfs_ss_either_flag_implies_global
core.math.s_definable.class_s_s	msfs_ss_local_implies_global
core.net.h3.request	H3Status
core.net.http2	Frame
core.net.quic.stream_sm	RecvError
core.net.quic.stream_sm	SendError
core.security.provenance	RekorInclusionProof
core.sys.darwin.libsystem	close
core.sys.darwin.libsystem	kevent
core.sys.darwin.libsystem	kqueue
core.sys.darwin.libsystem	open
