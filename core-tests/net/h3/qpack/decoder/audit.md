# core-tests/net/h3/qpack/decoder — audit
`core/net/h3/qpack/decoder.vr` — RFC 9204 QPACK field-line decoder. QpackDecodeError 5-variant (Truncated/InvalidIndex(UInt64)/DynamicTableNotSupported/HuffmanError/IntegerError): ctors + Eq + payload + disjointness (7 @test). Qualified Type.Variant. Live decode loop deferred to L2.
