# core-tests/net/quic/stream — audit
`core/net/quic/stream.vr` — RFC 9000 §2.1 stream direction. StreamDirection 2-variant (Bidirectional/Unidirectional) match-based discrimination (3 @test). QuicStream record + flow control deferred to L2.
