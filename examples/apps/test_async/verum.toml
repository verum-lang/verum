[package]
name = "test_async"
version = "1.0.0"
authors = ["Verum Team"]
description = "Comprehensive test for Verum async/await syntax"

[language]
profile = "application"

[dependencies]
stdlib = "0.1"

[features]
default = []
