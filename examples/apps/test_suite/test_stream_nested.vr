// Test: Nested stream comprehension
// Tests if stream comprehension can be nested

fn main() {
    let matrix = [[1, 2], [3, 4], [5, 6]];
    let flattened = stream[y for x in matrix for y in x];

    for item in flattened {
        print(item);
    }
}
