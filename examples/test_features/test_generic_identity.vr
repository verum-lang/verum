fn identity<T>(x: T) -> T {
    x
}

fn main() {
    let i = identity(42)
    let s = identity("hello")
    let f = identity(3.14)
    println(f"Int: {i}")
    println(f"Text: {s}")
    println(f"Float: {f}")
}
