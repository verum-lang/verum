// Working context system test with actual implementations

context Logger {
    fn log(message: Text)
}

// Define a record type for our logger implementation
type ConsoleLogger = {}

// Implement logger methods (this will fail without protocol impl syntax)
fn greet(name: Text) using Logger {
    Logger.log(f"Hello, {name}!")
}

fn main() {
    // Simple test without provide - just test context parsing
    println("Context declaration parsed successfully")

    // Test function with context requirement
    // This will fail at runtime because Logger isn't provided
    // greet("World")
}
