fn main() {
    // Test 2: Tuple patterns
    let pair = (1, 2);
    match pair {
        (0, 0) => print(f"origin"),
        (x, y) => print(f"point: {x}, {y}"),
    }
    print(f"Tuple pattern test complete!");
}
