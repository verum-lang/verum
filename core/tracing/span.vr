// =============================================================================
// tracing/span.vr — re-export shim
// =============================================================================
// The implementation lives in tracing/data.vr (single-module core: see
// the note there on cross-module record-literal type-id collisions).
// This shim preserves the public module path.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.tracing.span;

public mount core.tracing.data.Span;
public mount core.tracing.data.SpanInner;
public mount core.tracing.data.SPAN_EVENT_LIMIT;
public mount core.tracing.data.SPAN_LINK_LIMIT;
