// =============================================================================
// core/database/sqlite/native/integrity_walker_api/mod.vr
// =============================================================================
// PRAGMA integrity_check — tree walk that inspects every page and emits a
// list of findings. The walker tracks per-check categories so callers can
// classify output severity.
// Spec §10.7 — integrity_check.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.integrity_walker_api;

public module finding;
public module walker;
