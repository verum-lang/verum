// =============================================================================
// core/database/sqlite/native/truncate_optimization/mod.vr
// =============================================================================
// Truncate-optimization — DELETE FROM <table> (no WHERE, no triggers, no FK)
// is compiled to a single btreeClearTable opcode instead of a row-by-row scan.
// Spec §13.5.8 — DELETE optimizations.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.truncate_optimization;

public module eligibility;
