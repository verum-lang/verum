// =============================================================================
// core/database/sqlite/native/progress_handler_api/mod.vr
// =============================================================================
// sqlite3_progress_handler(db, N, callback, arg) — invoked every N VDBE
// instructions while a stmt is executing. Returning non-zero from the
// callback aborts the stmt with SQLITE_INTERRUPT. Spec App E.P.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.progress_handler_api;

public module handler;
