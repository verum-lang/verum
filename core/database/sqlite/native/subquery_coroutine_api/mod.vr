// =============================================================================
// core/database/sqlite/native/subquery_coroutine_api/mod.vr
// =============================================================================
// Co-routine subquery — an inline (SELECT ...) in the FROM clause compiled to
// an OP_InitCoroutine + OP_Yield, so rows are materialized lazily.
// Spec §13.5.9 — co-routine subqueries.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.subquery_coroutine_api;

public module coroutine;
