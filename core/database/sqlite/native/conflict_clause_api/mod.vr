// =============================================================================
// core/database/sqlite/native/conflict_clause_api/mod.vr
// =============================================================================
// ON CONFLICT action — SQLite constraint-resolution strategy.
// Spec §17.8 — constraint handling.
//

// Five actions: ROLLBACK / ABORT / FAIL / IGNORE / REPLACE.
// Used in CREATE TABLE (column/table constraint), INSERT OR ..., UPDATE OR ...,
// and PRAGMA default_conflict.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.conflict_clause_api;

public module action;
