// =============================================================================
// core/database/sqlite/native/changes/mod.vr
// =============================================================================
// Layer: 7 (façade — public row-accounting surface)
// Spec: SQLite C API — row counters and last insert rowid.
//
// Submodules:
//   * `counter`   — per-connection counters (changes / total / pending)
//   * `snapshot`  — immutable snapshot view for sqlite3_*_changes64
//   * `engine`    — pure accessors matching the C-API call names
// =============================================================================

module core.database.sqlite.native.changes;

public module counter;
public module snapshot;
public module engine;
