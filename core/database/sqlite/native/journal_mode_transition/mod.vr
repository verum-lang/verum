// =============================================================================
// core/database/sqlite/native/journal_mode_transition/mod.vr
// =============================================================================
// PRAGMA journal_mode — transition matrix between the six modes (DELETE /
// TRUNCATE / PERSIST / MEMORY / WAL / OFF).
// Spec §18.4 — journal_mode semantics.
//

// This catalogue complements the existing journal_mode taxonomy with a
// state-transition planner that captures which transitions need an exclusive
// lock + truncate, and which can happen lazily.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.journal_mode_transition;

public module action;
