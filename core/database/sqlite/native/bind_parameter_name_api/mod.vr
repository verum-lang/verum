// =============================================================================
// core/database/sqlite/native/bind_parameter_name_api/mod.vr
// =============================================================================
// sqlite3_bind_parameter_name(stmt, idx) — reverse of _index: returns the
// parameter name token (with sigil) for the given 1-based slot, or NULL for
// anonymous `?` parameters. Spec App E.B.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.bind_parameter_name_api;

public module reverse;
