// =============================================================================
// core/database/sqlite/native/automatic_index_pragma/mod.vr
// =============================================================================
// PRAGMA automatic_index — controls whether the planner may build transient
// indices ("automatic index") to speed up sub-optimal joins.  Default: ON.
// Disabling forces the planner to fall back to nested-loop scans.
// Spec §13.3 / §15.4.
// =============================================================================

module core.database.sqlite.native.automatic_index_pragma;

public module gate;
