// =============================================================================
// core/database/sqlite/native/nocase_ascii_fold/mod.vr
// =============================================================================
// SQLite NOCASE collation — ASCII-only upper/lower fold. This catalogue
// documents the folding table that the BINARY vs NOCASE diff relies on,
// and exposes exact fold helpers.
// Spec §15.4.2 — NOCASE.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.nocase_ascii_fold;

public module fold;
