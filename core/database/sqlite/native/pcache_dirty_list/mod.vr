// =============================================================================
// core/database/sqlite/native/pcache_dirty_list/mod.vr
// =============================================================================
// Page-cache dirty-page LRU bookkeeping — SQLite flushes pages in LRU order
// when cache pressure hits the spill threshold.
// Spec §10.4 — page cache.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.pcache_dirty_list;

public module state;
public module list;
