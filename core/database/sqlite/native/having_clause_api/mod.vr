// =============================================================================
// core/database/sqlite/native/having_clause_api/mod.vr
// =============================================================================
// HAVING clause semantics — post-aggregation filter that may reference
// aggregate functions or GROUP BY keys, but NOT bare column references.
// Spec §13.7 — GROUP BY + HAVING.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.having_clause_api;

public module reference;
public module rule;
