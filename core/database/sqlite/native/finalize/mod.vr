// =============================================================================
// core/database/sqlite/native/finalize/mod.vr
// =============================================================================
// Layer: 7 (façade — statement tear-down / reset)
// Spec: SQLite C API — sqlite3_finalize / sqlite3_reset /
//  sqlite3_clear_bindings.
//

// Submodules:
//  * `action` — which operation is being performed
//  * `outcome` — normalised return (OK vs ABORT-FROM-LAST-STEP)
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.finalize;

public module action;
public module outcome;
