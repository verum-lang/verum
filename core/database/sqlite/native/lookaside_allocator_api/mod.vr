// =============================================================================
// core/database/sqlite/native/lookaside_allocator_api/mod.vr
// =============================================================================
// Per-connection lookaside allocator — fixed-slot fast-path for small allocations.
// Spec §10.4 + §16 — memory management.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.lookaside_allocator_api;

public module config;
public module stats;
