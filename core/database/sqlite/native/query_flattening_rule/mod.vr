// =============================================================================
// core/database/sqlite/native/query_flattening_rule/mod.vr
// =============================================================================
// SQLite subquery-flattening — per optimizer §13.5 — 19 preconditions that
// block FROM-subquery inlining. Encoded as predicates so the planner can
// short-circuit at the first hit and produce a precise rejection reason.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.query_flattening_rule;

public module rule;
public module context;
