// =============================================================================
// core/database/sqlite/native/changeset_apply_policy/mod.vr
// =============================================================================
// sqlite3changeset_apply per-conflict policy — the xConflict callback returns
// OMIT / REPLACE / ABORT, and sqlite3changeset_apply_v2 accepts flag bits
// SQLITE_CHANGESETAPPLY_NOSAVEPOINT / _INVERT / _IGNORENOOP / _FKNOACTION.
// Spec §22 — session extension.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.changeset_apply_policy;

public module flags;
public module policy;
