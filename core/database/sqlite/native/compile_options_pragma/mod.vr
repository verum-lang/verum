// =============================================================================
// core/database/sqlite/native/compile_options_pragma/mod.vr
// =============================================================================
// PRAGMA compile_options — emits one row per SQLITE_ENABLE_* / OMIT_* / DEFAULT_*
// flag the build was compiled with. Equivalent to sqlite3_compileoption_get(N).
// Spec App E.C.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.compile_options_pragma;

public module flags;
