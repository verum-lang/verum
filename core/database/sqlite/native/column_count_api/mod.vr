// =============================================================================
// core/database/sqlite/native/column_count_api/mod.vr
// =============================================================================
// sqlite3_column_count(stmt) — number of result columns the statement will
// produce. Stable from prepare time. Returns 0 for non-SELECT. Spec App
// E.C.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.column_count_api;

public module count;
