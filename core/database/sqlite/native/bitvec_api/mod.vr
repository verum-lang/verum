// =============================================================================
// core/database/sqlite/native/bitvec_api/mod.vr
// =============================================================================
// Sparse bitmap for rowid sets (mirrors bitvec.c). Used by VACUUM and the
// VDBE's "seen" sets during joins / DELETE-by-rowid.
// Spec §11.5 — bitvec.
// =============================================================================

module core.database.sqlite.native.bitvec_api;

public module node;
public module bitvec;
