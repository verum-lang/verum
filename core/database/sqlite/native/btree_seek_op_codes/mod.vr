// =============================================================================
// core/database/sqlite/native/btree_seek_op_codes/mod.vr
// =============================================================================
// B-tree cursor seek-op codes — sqlite3BtreeMovetoUnpacked direction hints.
// Spec §12.5 — cursor seek.
//

//  GE — find smallest key >= target
//  LE — largest key <= target
//  EQ — exact match (else miss)
//  GT — strict greater
//  LT — strict less
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.btree_seek_op_codes;

public module op;
