// =============================================================================
// core/database/sqlite/native/vfs_xcurrenttime_api/mod.vr
// =============================================================================
// VFS clock methods — xCurrentTime (Julian-day Float, deprecated) and
// xCurrentTimeInt64 (milliseconds since 1970, recommended since 3.7.x).
// Used by date functions when "now" is requested. Spec §6.1.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.vfs_xcurrenttime_api;

public module clock;
