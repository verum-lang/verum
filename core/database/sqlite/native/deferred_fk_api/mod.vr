// =============================================================================
// core/database/sqlite/native/deferred_fk_api/mod.vr
// =============================================================================
// Deferred foreign-key enforcement — DEFERRABLE INITIALLY DEFERRED semantics.
// Spec §17.3 — FOREIGN KEY.
//

// SQLite only ever enforces at COMMIT boundary for DEFERRED constraints.
// This catalogue tracks the violation-set per-transaction.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.deferred_fk_api;

public module constraint_mode;
public module violation_set;
