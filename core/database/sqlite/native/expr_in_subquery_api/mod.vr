// =============================================================================
// core/database/sqlite/native/expr_in_subquery_api/mod.vr
// =============================================================================
// WHERE x IN (SELECT ...) subquery plan — materialized ephemeral table vs
// correlated lookup vs row-by-row scan.
// Spec §13.5.10 — IN-subquery planning.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1"),
)
module core.database.sqlite.native.expr_in_subquery_api;

public module strategy;
