// =============================================================================
// core/database/sqlite/native/unixepoch_fn/mod.vr
// =============================================================================
// unixepoch(time-value, modifier1, modifier2, ...) — added 3.38.  Returns the
// number of seconds since 1970-01-01 00:00:00 UTC for the parsed time-value
// (after applying modifiers).  Returns NULL if the time-value is invalid.
// Spec §15.6.
// =============================================================================

module core.database.sqlite.native.unixepoch_fn;

public module fn_;
