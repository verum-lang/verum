// =============================================================================
// core/database/sqlite/native/data_count_api/mod.vr
// =============================================================================
// sqlite3_data_count(stmt) — number of columns in the most recently fetched
// row. Differs from sqlite3_column_count: the latter is fixed from prepare;
// the former returns 0 unless the most recent step() returned ROW. Spec App
// E.C.
// =============================================================================

@arch_module(
    foundation: Foundation.ZfcTwoInacc,
    stratum: MsfsStratum.LFnd,
    lifecycle: Lifecycle.Theorem("v0.1")
)
module core.database.sqlite.native.data_count_api;

public module probe;
